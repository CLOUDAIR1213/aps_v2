# APS v2 面试项目介绍

## 1. 项目名称

**APS v2 - 轻量级生产排产系统**

## 2. 项目简介

本项目是一个面向机械加工、小批量多品种生产场景的轻量级 APS（Advanced Planning and Scheduling，高级计划与排程）系统。

在实际生产中，企业的工艺数据通常来源于 Excel，计划员需要人工判断：

- 每张订单由哪些零件和工序组成。
- 子件、父件之间的装配先后关系。
- 不同任务应该安排给哪些人员。
- 外协任务什么时候返回、是否会影响交期。
- 哪些订单可能延期，延期原因是什么。

本系统将这些过程系统化：从工艺 Excel 导入开始，解析订单、零件、工序和装配依赖，结合人员产能、工作日历、外协周期和订单交期自动生成排产方案，并提供派工、排班、甘特图、风险看板和 Excel 导出能力。

## 3. 项目实际意义

### 3.1 对计划员的价值

传统场景中，计划员往往需要在多个 Excel 表中反复核对工序、人员、交期和外协情况。系统将这一过程整合为统一工作流：

```text
基础配置 -> 工艺表导入 -> 自动排产 -> 派工与外协维护 -> 交付风险复核
```

计划员可以更快确定订单预计完工时间，减少人工排程和反复计算造成的错误。

### 3.2 对现场执行的价值

系统能够将排产任务分配到具体人员，并以生产排班表和甘特图展示任务时间安排，让现场人员明确：

- 谁负责该任务。
- 任务何时开始、何时结束。
- 当前是否存在未派工任务。
- 外协任务是否会阻塞后续生产。

### 3.3 对管理人员的价值

管理人员不需要逐单查看全部工序，而是可以通过交付风险看板直接关注：

- 哪些订单预计延期。
- 哪些订单临近交期。
- 哪些资源负荷过高。
- 哪些关键工序影响交付。
- 哪些外协任务可能造成延期。

项目的核心业务价值可以概括为：

> 将依赖计划员经验的人工排产过程，转化为可计算、可解释、可反馈重算的生产计划闭环。

## 4. 核心业务流程

```text
维护工段、设备、人员和工序映射
        |
        v
上传并预览工艺 Excel
        |
        v
解析零件、工序和父子装配依赖
        |
        v
按人员产能、交期和工作日历生成排产方案
        |
        v
查看订单完工时间、瓶颈和延期风险
        |
        v
进行人员派工分摊和外协状态维护
        |
        v
根据反馈重算后续计划
        |
        v
排班表、甘特图、风险看板和 Excel 导出
```

## 5. 主要功能

### 5.1 基础数据管理

- 工段和设备配置。
- 人员档案及人员与工段关联。
- 工序映射规则管理，将 Excel 工序列映射到系统工段。
- 资源分组管理。

### 5.2 工单导入

- 上传机械加工工艺 Excel。
- 导入前预览零件、工序、工时和异常数据。
- 支持识别外协工序。
- 支持解析 `NO` 编号表达的父子装配层级。
- 将工艺数据提交为系统中的工单、零件、工序和依赖关系。

### 5.3 自动排产

- 按订单优先级、交期和导入时间排序。
- 处理工序前后置依赖。
- 处理父子装配依赖。
- 以人员作为内部工序的主要产能约束，避免同一人员任务时间重叠。
- 按固定班制处理午休、下班和周日休息。
- 支持外协任务及外协并发时段。
- 每次运行生成新的排产方案，保留历史方案。

### 5.4 派工与外协反馈

- 将内部排产明细分配给一名或多名人员。
- 人员工时占比合计必须为 100%。
- 支持人员工时汇总及 Excel 导出。
- 支持维护外协任务的待发出、已发出、已返回和异常状态。
- 外协预计返回或实际返回变化后，自动重算受影响的后续任务。

### 5.5 结果分析与风险管理

- 订单完工表：展示预计开始、预计完成、延期天数和主要瓶颈。
- 订单排产详情：解释工序顺序、前置关系和派工状态。
- 生产排班表：按日期复核生产任务。
- 甘特图：查看人员及外协任务时间轴。
- 交付风险看板：识别延期、临近交期、资源瓶颈、关键工序阻塞和外协风险。
- 排产结果和风险报告 Excel 导出。

## 6. 技术架构

### 6.1 技术栈

| 模块 | 技术 |
| --- | --- |
| 后端框架 | FastAPI |
| ORM | SQLAlchemy Async |
| 数据库 | MySQL 8.0 |
| 数据库迁移 | Alembic |
| Excel 处理 | openpyxl |
| 前端框架 | React |
| 前端构建 | Vite |
| 路由管理 | React Router |
| HTTP 请求 | Axios |

### 6.2 核心数据模型

| 模型 | 作用 |
| --- | --- |
| `WorkOrder` | 工单主数据，包含客户、产品、优先级和交期 |
| `Part` | 零件及父子装配层级 |
| `ProductionOperation` | 排产的最小工序任务 |
| `OperationDependency` | 工序之间的前后置依赖 |
| `Personnel` | 生产人员数据 |
| `WorkCenter` | 工段及外协工段 |
| `ProductionSchedule` | 排产方案版本 |
| `ProductionScheduleItem` | 方案中的具体任务时间安排 |
| `ProductionScheduleItemPersonnelAllocation` | 排产任务到人员的工时分摊 |
| `BusinessRiskIssueState` | 风险问题的人工处理状态和备注 |

## 7. 项目技术难点

## 7.1 难点一：将非标准 Excel 工艺数据转换为可排产模型

### 问题

工艺 Excel 不是直接可执行的排产数据，其中既包含零件信息，也包含不同工序列和装配编号。特别是 `NO` 编号会表达零件的层级结构，例如：

| NO | 含义 |
| --- | --- |
| `1` | 一级总成 |
| `1.1` | `1` 的子件 |
| `1.1.1` | `1.1` 的子件 |

如果只按照 Excel 行顺序排产，就可能出现父件已经开始装配，但子件还没有加工完成的问题。

### 解决方案

系统在导入阶段完成两类转换：

1. 同一零件内部的工序按列顺序生成顺序依赖。
2. 父件第一道工序依赖所有直接子件的完成工序。

例如：

```text
子件最后一道工序 -> 父件第一道工序
```

这样，导入模块负责把业务语义转换成依赖图，排产引擎只需要处理统一的 `OperationDependency`，不需要关心 Excel 原始格式。

### 面试表达

> 我把 Excel 的层级语义在导入阶段转换成统一的工序依赖图，这样排产算法和数据来源解耦。以后即使导入来源变化，只要能生成依赖关系，排产引擎就可以复用。

## 7.2 难点二：在多种约束下生成可执行的排产方案

### 问题

生产任务不能只按工时顺序排列，还需要同时满足：

- 工序依赖约束。
- 父子装配约束。
- 人员不能在同一时间处理两个任务。
- 工时需要按零件数量折算。
- 午休、下班和周日不能安排内部生产。
- 外协任务需要影响完工时间，但不能占用内部人员。

### 解决方案

系统采用规则排产方式：

- 从依赖已满足的任务中选择下一条可排任务。
- 任务排序规则为：订单优先级高、交期早、导入时间早。
- 内部工序查找对应工段下最早可用的在职人员。
- 根据人员当前可用时间和前置工序结束时间确定任务开始时间。
- 通过工作日历函数处理跨午休、跨下班和周日顺延。
- 外协工序独立维护并发槽位，不占内部人员，但参与整个交付时间计算。

### 面试表达

> 当前版本没有直接使用求解器，而是采用可解释的规则排产。原因是业务第一阶段更关注方案能否落地、为什么这样排以及能否被计划员理解和调整。等物料、设备维护和复杂插单约束稳定后，再考虑引入优化算法。

## 7.3 难点三：派工和外协反馈变化后的局部重算

### 问题

真实生产计划不是生成一次就不变。例如：

- 某个任务原本由一人完成，后来改为多人协作。
- 外协任务预计返回时间推迟。
- 外协实际提前返回。

这些变化会影响后续依赖任务和订单完工时间。如果只修改当前记录，而不同步后续计划，就会导致排班表、甘特图和风险看板数据不一致。

### 解决方案

系统实现了基于当前排产方案的后继任务重算：

- 保存派工分摊后，从被修改任务开始重算后续任务。
- 修改外协预计或实际返回时间后，从该外协任务开始重算后续依赖链。
- 订单完工表、排班表、甘特图、人员负荷和风险看板统一读取同一个方案数据。

### 面试表达

> 我重点处理了计划反馈后的数据一致性问题。派工和外协状态变化后，系统不是仅更新一个字段，而是重新传播时间影响，保证所有结果页面看到的是同一版可执行计划。

## 8. 项目亮点

### 8.1 从数据导入到风险管理的完整闭环

本项目不是单独的排产算法演示，而是覆盖了生产计划完整链路：

```text
工艺导入 -> 依赖构建 -> 自动排产 -> 人员派工 -> 外协反馈 -> 风险识别 -> 导出复核
```

该闭环更贴近企业真实使用流程，也比单纯实现增删改查更有业务价值。

### 8.2 排产结果能够解释，而不是只展示时间

系统能够从订单维度展示：

- 预计完工时间。
- 延期天数。
- 最后完成的关键工序。
- 可能的瓶颈工段。
- 工序前置关系及派工状态。

对于生产系统来说，计划员不仅需要一个结果，更需要理解结果产生的原因，以便做调整和沟通。

### 8.3 将外协作为真实生产约束处理

很多简单排产项目只考虑内部设备或人员，而机械加工业务中，外协往往直接决定交期。本系统支持：

- 外协默认周期。
- 外协并发处理能力。
- 送出、预计返回、实际返回和异常状态。
- 外协返回变化后的后续计划重算。

这使项目更符合实际生产现场。

### 8.4 风险动态计算，人工状态单独保存

系统没有将全部风险结果固化到数据库中，而是：

- 根据当前排产方案动态计算风险。
- 只保存人工维护的处理状态和备注。

这种设计可以避免排产方案变化后出现历史风险过期、重复存储和文案难维护的问题。

## 9. 项目边界与不足

面试中应主动说明当前系统的边界，这比夸大功能更可信。

- 当前未接入 OR-Tools 等优化求解器，使用的是可解释规则排产。
- 当前未支持物料库存齐套约束。
- 当前未支持节假日、加班审批和设备维护日历。
- 当前不是完整 MES，不包含实际报工和工时偏差分析。
- 整单锁定已支持状态维护，但锁定计划还没有成为重排过程中的硬约束。
- 当前自动化测试覆盖了层级依赖和外协关键规则，但还没有建立完整测试体系。

## 10. 两分钟口述版本

我做的是一个面向机械加工企业的轻量级 APS 生产排产系统。这个业务的特点是小批量、多品种，工艺数据主要存在 Excel 中，计划员通常需要人工判断零件装配关系、人员是否冲突、外协是否影响交付以及订单什么时候能完成。

项目的流程是先导入工艺 Excel，把订单拆分成零件和工序，并根据 `NO` 编号识别父子装配关系，生成工序依赖图。之后系统根据订单优先级、交期、人员可用时间和工作日历自动生成排产方案。排产结果可以在订单完工表、排班表和甘特图中查看，还可以进行人员派工和外协任务维护。

这个项目技术上最难的地方有三个。第一是把非标准 Excel 中的层级业务关系转换成统一的依赖图，保证父件必须等待子件完成后才能开始。第二是排产过程中同时处理工序依赖、人员时间冲突、午休下班顺延以及外协并发时段。第三是当派工或外协返回时间变化时，对后续计划进行重算，并保证完工时间、甘特图和风险看板的数据一致。

我认为项目最大的亮点是它不是只生成一张排产表，而是形成了从导入、排产、派工、外协反馈到交付风险识别的闭环。管理人员可以直接看到哪些订单可能延期以及主要原因，计划员也能根据反馈及时调整方案。

当前版本采用的是规则排产，重点是可解释和可落地；物料库存、设备维护日历和优化求解器属于后续可以继续扩展的方向。

## 11. 一分钟精简版本

我的项目是一个面向机械加工小批量生产的 APS 排产系统。它可以从工艺 Excel 导入订单、零件和工序，根据零件编号生成装配依赖，再结合人员产能、工作日历、订单交期和外协周期自动生成排产方案。

我重点解决了三个问题：一是 Excel 层级结构如何转换成工序依赖图；二是在人员冲突、工序顺序和外协约束下如何生成可执行计划；三是派工或外协反馈变化后如何重算后续任务并同步交付风险。

项目最有价值的地方是形成了从工艺导入、排产、派工、外协管理到延期风险分析的闭环，而不是简单做一个数据管理系统。

## 12. 常见追问与回答

### 12.1 为什么没有直接使用 OR-Tools？

当前阶段首先要解决的是基础数据不稳定、业务规则需要解释以及计划员需要能看懂和操作的问题。因此先使用规则排产，更容易验收和落地。等物料约束、设备维护、插单和优化目标进一步明确后，再引入求解器更合理。

### 12.2 为什么把依赖关系在导入阶段生成？

因为 Excel 的编号层级属于输入数据语义。如果排产阶段直接解析 Excel 结构，算法会和文件格式高度耦合。导入阶段将其转成标准依赖图后，排产模块只面向统一数据模型工作，更容易维护和扩展。

### 12.3 为什么风险结果不全部存数据库？

风险取决于排产方案。方案一旦重算，旧风险结果就可能失效。因此系统动态计算风险，只保存用户处理状态和备注，可以减少数据冗余并保证风险与当前方案一致。

### 12.4 为什么当前以人员作为主要产能约束？

当前场景需要重点解决派工和人员工作量安排问题，因此内部任务以人员可用时间作为主要约束；设备信息目前用于基础配置和辅助展示。未来可以进一步把设备维护和设备并行能力纳入硬约束。

### 12.5 你在项目中最大的收获是什么？

我最大的收获是理解了业务系统不能只完成页面和接口，还要把业务规则转化成稳定的数据模型，并保证反馈变化后整个链路的数据一致。尤其是生产排产系统，算法结果必须能够解释、调整并反馈到风险判断中。

## 13. 代码依据

| 能力 | 核心文件 |
| --- | --- |
| Excel 工艺解析 | `backend/app/services/production_import_service.py` |
| 工序依赖构建与排产 | `backend/app/services/production_service.py` |
| 风险看板动态计算 | `backend/app/services/management_dashboard_service.py` |
| 结果分析 | `backend/app/services/production_analysis_service.py` |
| 生产主线模型 | `backend/app/models/production.py` |
| 派工页面 | `frontend/src/pages/Dispatch.jsx` |
| 外协管理页面 | `frontend/src/pages/ExternalTasks.jsx` |
| 当前方案上下文 | `frontend/src/utils/scheduleContext.js` |
| 层级依赖测试 | `backend/tests/test_production_hierarchy.py` |
| 外协解析与并发测试 | `backend/tests/test_external_import_and_slots.py` |

