# APS v2 派工批量化与同类工序工时汇总专项计划

## Summary

本文档用于定义 APS v2 派工与派班模块的局部优化计划，重点是“同类工序合并工时、批量选择、批量派工、人员工时汇总、生产排班表展示、加工单导出”的统一口径。

本文档不是 `plan4`，不作为 APS v2 第四阶段主线，不替代 `plan2.md` 或 `plan-ui-refactor.md`，也不写入 `README.md`。当前 `README.md` 只记录真实实现状态；本文只记录下一轮派工专项优化需求。

## 当前问题

当前派工页已经支持单条任务人员分摊、一键分配预览、人员工时汇总和导出，但计划员在处理大量相同工序时仍然会遇到几个问题：

- 同一方案中，同工段、同工序名的任务分散在多条排产任务里。
- 计划员需要反复点击相似任务，再重复设置相同人员分摊。
- 一键分配能提高速度，但计划员缺少“先按工序看总工时，再决定怎么派”的操作视图。
- 人员工时汇总已有，但需要和批量派工后的总工时、任务明细、排班表展示保持一致。
- 生产排班表必须继续能追溯到原始任务，不能因为界面合并而丢失订单、零件、时间信息。

## 目标

第一版目标是提高派工效率，同时保证工时口径和原始任务追溯不被破坏：

- 支持按同类工序汇总查看待派工任务。
- 支持批量选择同类工序任务。
- 支持对已选任务统一设置人员分摊。
- 批量保存后，每条原始 `schedule_item` 都有自己的人员分摊记录。
- 人员工时汇总按原始任务分摊后的 `planned_minutes` 计算，不重复、不漏算。
- 生产排班表默认仍展示原始任务，保证现场复核和追溯。

## 合并口径

第一版固定使用以下合并口径：

```text
同方案 + 同工段 + 同工序名
```

说明：

- 同方案：只在当前 `ProductionSchedule` 内汇总，不跨方案。
- 同工段：使用 `work_center_id` 作为判断依据，不只看工段名称。
- 同工序名：使用排产任务关联的工序名称作为判断依据。
- 不要求同订单。
- 不要求同零件。
- 不要求同日期。
- 不要求时间连续。

该合并只是派工页的操作视图，不新增真实“合并任务”主模型。

## 派工页交互

派工页增加“按工序汇总”视图，与现有任务明细视图并存。

汇总组至少显示：

- 工段。
- 工序名。
- 任务数。
- 总计划工时。
- 涉及订单数。
- 未派工任务数。
- 待补足任务数。
- 已完整派工任务数。
- 当前派工状态。

汇总组交互：

- 支持展开查看组内原始任务。
- 支持整组选择。
- 支持只选择组内部分任务。
- 支持按当前筛选条件批量选择。
- 支持清空选择。

右侧人员分摊面板在批量模式下显示：

- 已选任务数。
- 已选任务总工时。
- 涉及工段。
- 涉及工序。
- 涉及订单数。
- 人员分摊比例。
- 每名人员预计分摊总工时。

## 批量保存规则

批量派工保存时：

- 同一套人员占比应用到被选中的每条原始 `schedule_item`。
- 每条原始任务仍保存到 `production_schedule_item_personnel_allocations`。
- 不新增批量派工主表。
- 不新增真实合并任务表。
- 不改变 `production_schedule_items` 的原始任务粒度。
- 保存后刷新派工任务列表、人员工时汇总和生产排班表相关数据。

默认覆盖规则：

- 第一版默认不覆盖已完整派工任务。
- 未派工任务可以被批量派工。
- 待补足任务可以被批量补足。
- 已完整派工任务如需覆盖，后续必须单独增加“允许覆盖已派工任务”开关，并在预览中明确提示。

失败处理：

- 如果已选任务为空，不允许保存。
- 如果人员占比合计不是 100%，不允许保存。
- 如果同一任务中选择重复人员，不允许保存。
- 如果人员不是在职状态，不允许保存。
- 如果某些任务已完整派工且未开启覆盖，预览中应显示跳过原因。

## 人员工时汇总

人员工时汇总仍以原始任务为准：

- 汇总总工时 = 所有原始任务分摊后的 `planned_minutes` 之和。
- 任务数 = 参与分摊的原始任务数。
- 涉及订单数 = 原始任务关联订单去重数量。
- 涉及工段数 = 原始任务关联工段去重数量。
- 明细仍展示原始订单、工序、计划开始、计划结束、分摊比例、分摊工时。

批量派工后必须立即刷新人员工时汇总。

导出规则：

- 人员工时汇总导出仍包含人员汇总和任务明细。
- 任务明细不只显示合并组，必须能追溯到原始 `schedule_item`。
- 第一版不新增“按工序汇总导出”作为必须项。

## 生产排班表展示

生产排班表第一版仍默认展示原始任务：

- 人员视图继续按真实人员分摊展示。
- 每个单元格仍能看到原始订单、工序、时间段和工时。
- 批量派工后，排班表按保存后的人员分摊结果刷新。
- 不因为派工页的合并视图而隐藏原始任务。

后续可扩展：

- 增加“按同类工序合并显示”开关。
- 在排班表中提供合并组摘要。
- 支持从排班表合并组反查原始任务。

这些扩展不作为第一版必须实现。

## 后端接口口径

第一版优先复用现有派工与人员工时结构。

建议最小新增能力：

- 派工数据返回时可由前端按 `work_center_id + operation_name` 分组。
- 批量保存可以新增批量接口，也可以前端循环调用现有单任务保存接口；正式实现前需评估性能和一致性。
- 如果新增批量接口，接口语义应是“对一组原始 `schedule_item_id` 保存同一套人员分摊”，不是“保存合并任务”。

批量接口建议输入：

```text
schedule_item_ids: number[]
allocations: [{ person_id, ratio_percent }]
overwrite_assigned: false
```

批量接口建议输出：

```text
processed_count
skipped_count
skipped_items
recalculated_item_count
```

## 前端实现口径

派工页第一版建议保留两个视图：

- 明细视图：沿用当前单任务派工流程。
- 按工序汇总视图：用于同类工序合并工时和批量选择。

前端需要明确显示当前模式，避免计划员误以为系统真的生成了合并任务。

推荐文案：

```text
按工序汇总仅用于批量派工操作，保存后仍回写到每条原始任务。
```

## 实现状态（2026-06-01）

本节记录本专项计划第一轮实现后的实际代码状态，不替代上方需求口径。

已实现：

- 派工页保留“明细视图”，并新增“按工序汇总”视图。
- 前端在派工页按 `work_center_id + operation_name` 对当前派工任务分组，不新增真实合并任务模型。
- 汇总组显示工段、工序名、任务数、总计划工时、涉及订单数、未派工任务数、待补足任务数、已完整派工任务数和当前派工状态。
- 汇总组支持展开查看原始任务。
- 汇总视图支持整组选择、组内单条选择、按当前筛选结果批量选择和清空选择。
- 右侧人员分摊面板支持批量模式，显示已选任务数、已选任务总工时、涉及工段、涉及工序、涉及订单数、人员占比和每名人员预计分摊总工时。
- 后端新增批量派工保存接口：`PUT /api/production/scheduling/schedules/{schedule_id}/dispatch/personnel-allocations/batch`。
- 批量接口输入为 `schedule_item_ids`、`allocations`、`overwrite_assigned`，语义是对多条原始 `schedule_item` 保存同一套人员分摊。
- 批量保存仍写入 `production_schedule_item_personnel_allocations`，不新增批量派工主表，不新增真实合并任务表。
- 批量保存默认 `overwrite_assigned = false`，已完整派工任务会被跳过。
- 批量保存后会重算后续计划，并由派工页刷新派工任务列表和人员工时汇总。
- 人员工时汇总仍通过 `get_personnel_workload` 读取原始任务分摊后的 `planned_minutes`，明细继续追溯到原始 `schedule_item`。
- 生产排班表接口仍按原始排产任务生成数据；派工页合并视图不改变排班表默认粒度。

部分实现或限制：

- 批量保存接口会返回 `processed_count`、`skipped_count`、`skipped_items` 和 `recalculated_item_count`；当前前端只在成功消息中展示处理数和跳过数，尚未提供跳过任务明细展开面板。
- 已完整派工任务的跳过原因在接口返回结果中可用，但当前第一版没有保存前预览弹窗。
- `overwrite_assigned` 后端字段已预留，但前端没有提供“允许覆盖已派工任务”开关；这符合第一版默认不覆盖的口径。
- 当前没有新增“按工序汇总导出”，人员工时导出仍保持原始任务明细口径。

已验证：

- `python -m compileall backend/app` 通过。
- `PYTHONPATH=backend DATABASE_URL=sqlite+aiosqlite:///:memory: python -m pytest backend/tests` 通过，7 个测试通过。
- `npm run build` 通过。
- 直接使用默认 `DATABASE_URL` 跑后端测试时，本机缺少 `asyncmy` 会导致测试收集失败；使用 SQLite 测试 URL 可通过。

## 实现状态（2026-06-26，加工单中心增量）

本节记录派工后执行单据闭环的增量实现。该增量仍属于派工专项优化，不新增 APS v2 主线阶段。

已实现：

- 前端新增“加工单中心”页面：`frontend/src/pages/WorkOrderTickets.jsx`。
- 路由新增 `/work-order-tickets`，并在 `frontend/src/router.jsx`、`frontend/src/navigation.js` 中接入主流程导航。
- 派工页 `frontend/src/pages/Dispatch.jsx` 顶部增加“加工单中心”入口，便于派工后进入导出。
- 前端 API 新增 `getWorkOrderTickets` 和 `exportWorkOrderTickets`，对应 `frontend/src/api/production.js`。
- 后端 schema 新增 `WorkOrderTicketAllocation`、`WorkOrderTicketRow`、`WorkOrderTicketResponse`，对应 `backend/app/schemas/production.py`。
- 后端新增加工单列表接口：`GET /api/production/scheduling/schedules/{schedule_id}/work-order-tickets`。
- 后端新增加工单导出接口：`GET /api/production/scheduling/schedules/{schedule_id}/work-order-tickets/export`。
- 加工单中心支持按工单、工段、计划开始日期范围、加工单状态筛选。
- 加工单状态当前由人员分摊比例推导：
  - `pending_dispatch`：未派工。
  - `needs_completion`：已部分派工但占比未达到 100%。
  - `ready_to_export`：人员占比合计达到 100%，可导出。
- 正式导出只包含 `ready_to_export` 的内部任务；未派工和待补足任务可在页面查看，但不会进入 Excel。
- 导出 Excel 包含“加工单明细”和“人员任务明细”两个 sheet。
- 导出时写入现有 `export_batches` 表，`export_type = work_order_ticket`，并在 `params_json` 中记录筛选条件和导出的 `schedule_item_ids`。
- 加工单号当前按 `JG-{schedule_no}-{schedule_item_id}` 生成，不新增独立加工单主表。

部分实现或限制：

- 当前加工单导出是基于已完整派工任务的执行单据导出，不是完整 MES 工单生命周期。
- 当前没有新增“派工草稿 / 已确认派工 / 已下发 / 已作废”状态字段，也没有加工单版本表。
- `export_batches` 只记录导出批次和筛选快照，不保存每张加工单的完整字段快照；如果后续需要历史加工单不可变追溯，应新增加工单批次/明细表和迁移。
- 当前没有加工单 PDF 导出，第一版只提供 Excel。
- 当前没有现场扫码、开工、完工、异常报工闭环。
- 当前没有加工单作废、重打、下发确认流程。

已验证：

- `python -m compileall backend\app\schemas\production.py backend\app\services\production_service.py backend\app\api\production.py` 通过。
- `npm run build` 通过。
- `npm run test:nav` 通过。

## 不做范围

第一版不做：

- 不创建真实合并任务表。
- 不创建批量派工主表。
- 不做复杂班组。
- 不做技能矩阵。
- 不做请假审批。
- 不做加班审批。
- 不做现场报工。
- 不做加工单下发、作废、版本追溯和现场扫码报工。
- 不改排产算法主流程。
- 不把生产排班表默认改成合并视图。

## 验收标准

文档验收：

- 文件名为 `plan-dispatch-batching.md`。
- 明确不是 `plan4`。
- 明确合并口径为同方案、同工段、同工序名。
- 明确不新增真实合并任务模型。
- 明确批量保存仍回写原始 `schedule_item`。

功能验收：

- 多条同方案、同工段、同工序名任务能合并显示总工时。
- 汇总组能展开看到原始任务。
- 可以整组选择，也可以选择组内部分任务。
- 批量选择后，人员分摊面板显示已选任务总工时。
- 批量派工后，每条原始任务都有人员分摊记录。
- 已完整派工任务默认不会被批量覆盖。
- 人员工时汇总不重复、不漏算。
- 人员工时汇总明细能追溯到原始任务。
- 生产排班表仍能追溯到原始任务。

## Assumptions

- 第一版以派工操作效率和工时汇总一致性为目标。
- 第一版不新增数据库表，优先复用 `production_schedule_item_personnel_allocations`。
- 第一版不改变 `ProductionScheduleItem` 粒度。
- 第一版不改变排产算法主流程。
- 第一版可先由前端分组展示；是否新增后端批量保存接口由实现阶段根据一致性和性能决定。
