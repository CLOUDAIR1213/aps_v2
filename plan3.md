# APS v2 第三阶段交付风险看板落地规划

## Summary

本文档用于记录 APS v2 第三阶段的落地计划。第三阶段承接 `PLAN1.md` 的核心基础数据和 `plan2.md` 的可控排产，不替代前两阶段，也不改变当前 `production_*` 主线。

第三阶段定位为：**老板交付风险看板**。

命名说明：历史规划中出现的“经营问题看板”现在前端统一显示为“交付风险看板”。接口路径、后端 service/schema 命名仍沿用 `management-dashboard`，避免为了改名重写系统。

它不是 KPI 大屏，也不是完整经营分析系统。它的核心价值是让老板和调度员一眼看到：

- 哪些订单会延期。
- 为什么延期。
- 哪个资源是瓶颈。
- 哪个工序卡住了后续流程。
- 外协是否影响交付。
- 哪些问题已经处理，哪些还没人处理。

当前确认的产品决策：

- 第三阶段先做老板交付风险视角。
- 默认看未来 30 天。
- 物料暂不做字段、不做约束、不做库存系统，只在后续规划中保留。
- 风险问题支持轻量处理状态：未处理、处理中、已处理、暂缓。
- 风险问题支持备注。
- 导出采用 Excel 多 Sheet。
- 页面默认展示问题清单，不做炫技大屏。

## 第三阶段定位

第一阶段解决“系统知道有什么基础数据”。

第二阶段解决“系统能可控排产，并知道每张订单什么时候完工”。

第三阶段解决“管理层能快速判断哪里出问题，以及先处理什么”。

第三阶段不追求展示更多图表，而是把排产结果、资源负荷、延期风险、外协影响和人工处理状态组织成经营问题清单。

第三阶段的默认用户是：

- 老板。
- 厂长。
- 生产经理。
- 计划主管。
- 调度员。

第三阶段要避免：

- 只展示总订单数、总工时、平均负荷率。
- 做成全屏大屏样式。
- 让老板从甘特图里自己找问题。
- 把库存、采购、执行反馈一次性全做进去。

## 用户与核心问题

### 老板关心的问题

老板打开页面后，应该能立即回答：

- 未来 30 天哪些订单有交付风险。
- 哪些订单已经延期。
- 哪些订单预计会延期。
- 哪些客户的订单风险最高。
- 最大瓶颈工段或设备在哪里。
- 哪些外协工序可能拖慢交付。
- 哪些问题还没有人处理。

### 调度员关心的问题

调度员打开页面后，应该能立即回答：

- 今天最应该先处理哪几张单。
- 哪些订单需要调整优先级或重新排产。
- 哪些资源已经接近过载。
- 哪些关键工序会影响整张订单完工。
- 哪些问题需要记录备注或标记处理中。

### 页面要回答的核心句式

每条问题最好能表达成一句话：

- `订单 WO-001 预计延期 3 天，主要卡在焊接。`
- `5m 龙门负荷过高，影响 4 张订单。`
- `订单 WO-008 的外协表面处理是最后完成工序，可能影响交付。`
- `工序“拼装”完成过晚，导致后续焊接和加工全部顺延。`

## 功能范围

### 第三阶段实现

- 新增经营问题看板页面。
- 默认读取最新排产方案，也支持选择历史排产方案。
- 默认时间范围为未来 30 天。
- 支持按风险等级、问题类型、客户、处理状态筛选。
- 支持风险问题处理状态和备注。
- 支持导出经营风险报告 Excel。
- 支持从问题跳转到订单详情、排班表或甘特图。

### 风险类型

第三阶段第一版风险类型：

- 订单延期风险。
- 临近交期风险。
- 资源瓶颈风险。
- 关键工序阻塞风险。
- 外协影响交付风险。

暂不实现：

- 物料字段。
- 库存齐套。
- 物料约束排产。
- 完整执行反馈。
- 人员绩效。
- 成本分析。
- PDF 报告。
- 供应商协同。
- 采购建议。

### 问题处理状态

第三阶段第一版支持轻量处理状态：

- `open`：未处理。
- `processing`：处理中。
- `resolved`：已处理。
- `paused`：暂缓。

处理状态的作用：

- 让老板知道问题是否有人处理。
- 让调度员能记录处理进度。
- 让导出报告能带上处理状态。

第一版不做：

- 责任人。
- 处理截止时间。
- 多条处理日志。
- 消息通知。
- 权限审批。

## 风险识别规则

### 风险等级

风险等级分为：

- `high`：高风险。
- `medium`：中风险。
- `low`：低风险。

中文展示：

- 高风险。
- 中风险。
- 低风险。

### 高风险规则

满足任一条件即可判定为高风险：

- 订单已经延期。
- 订单预计延期超过 2 天。
- 外协工序导致订单延期（计划完成晚于交期）。
- 资源负荷率 `>= 90%`，并且影响延期订单。
- 关键工序计划完成时间已经晚于订单交期。

### 中风险规则

满足任一条件即可判定为中风险：

- 订单预计延期 1 到 2 天。
- 交期 3 天内，仍有关键工序未完成或计划开始较晚。
- 资源负荷率在 `80% - 90%`，并且影响多个订单。
- 外协工序接近订单交期完成，留给后续工序的时间不足。

### 低风险规则

满足任一条件即可判定为低风险：

- 未来 30 天内存在交付压力，但当前预计仍可按期完成。
- 资源负荷偏高但暂未造成延期。
- 某工序是关键路径工序，但当前仍有处理余量。

### 风险类型规则

#### 订单延期风险

来源：

- `ProductionScheduleItem`
- `WorkOrder`
- 订单级排产总览结果

判断：

- 订单预计完成时间晚于交期。
- 延期天数按自然日计算。

输出：

- 订单号。
- 客户。
- 产品。
- 交期。
- 预计完成。
- 延期天数。
- 主要瓶颈。
- 原因。
- 建议动作。

#### 临近交期风险

判断：

- 订单交期在未来 30 天内。
- 当前预计完成时间距离交期过近。
- 或交期 3 天内仍存在关键工序计划未开始。

第一版由于没有执行反馈，不能判断真实“未完成”，只能基于计划开始和计划结束时间判断计划风险。

输出：

- 订单号。
- 客户。
- 交期。
- 预计完成。
- 剩余天数。
- 当前最晚工序。
- 建议动作。

#### 资源瓶颈风险

来源：

- 资源负荷接口。
- `ProductionScheduleItem`
- `ResourceMachine`
- `WorkCenter`

判断：

- 资源负荷率 `>= 90%` 为高风险瓶颈。
- 资源负荷率 `80% - 90%` 为中风险瓶颈。
- 需要统计该资源影响的订单数、工序数和延期订单数。

输出：

- 工段。
- 设备。
- 负荷率。
- 占用分钟。
- 可用分钟。
- 影响订单数。
- 影响延期订单数。
- 建议动作。

#### 关键工序阻塞风险

来源：

- `OperationDependency`
- `ProductionOperation`
- `ProductionScheduleItem`

第一版规则：

- 找每张订单的最后完成工序。
- 找依赖链中后续影响较多的工序。
- 找等待时间长、结束时间接近或晚于交期的工序。

可计算字段：

- 后续受影响工序数。
- 后续受影响零件数。
- 当前工序结束时间。
- 所属订单交期。

输出：

- 订单号。
- 零件。
- 工序。
- 工段。
- 设备。
- 计划开始。
- 计划结束。
- 后续受影响工序数。
- 原因。
- 建议动作。

#### 外协影响交付风险

来源：

- `WorkCenter.is_external`
- `ProductionScheduleItem.is_external`

判断：

- 外协工序导致订单延期（计划完成晚于交期）→ `high`。
- 外协工序结束时间距离交期过近（3 天内）→ `medium`。
- 外协仅为最后完成但未延期、未临近交期 → 不生成风险。

输出：

- 订单号。
- 客户。
- 外协工段。
- 零件。
- 工序。
- 预计开始。
- 预计完成。
- 是否最后完成工序。
- 是否导致延期。
- 建议动作。

### 建议动作模板

订单延期：

- `建议优先确认该订单交期承诺，必要时调整优先级或与客户沟通交付时间。`

资源瓶颈：

- `建议检查该资源是否可增加班次、临时分流、外协或调整订单优先级。`

关键工序阻塞：

- `建议优先处理该工序，避免后续工序继续顺延。`

外协风险：

- `建议尽快确认外协交付时间，必要时提前发出、催交或寻找备用外协。`

## 数据库规划

第三阶段不改旧 Demo 表，不新增重复排产表，继续以 `production_*` 为主线。

第三阶段新增一张轻量状态表，用于保存“系统计算出来的问题”的人工处理状态，不存完整重复风险快照。

### 新增表：business_risk_issue_states

用途：

- 保存某个排产方案下某条风险问题的处理状态。
- 保存处理备注。
- 不保存完整问题内容。

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | Integer | 主键 |
| schedule_id | Integer | 关联 `production_schedules.id` |
| issue_key | String(160) | 稳定问题标识 |
| status | String(30) | `open`、`processing`、`resolved`、`paused` |
| note | Text | 处理备注 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

建议约束：

- `schedule_id + issue_key` 唯一。
- `schedule_id` 外键关联 `production_schedules.id`。
- `status` 使用字符串，便于 SQLite 和 PostgreSQL 兼容。

建议关系：

- `ProductionSchedule.business_risk_issue_states`

### issue_key 规则

`issue_key` 必须稳定、可重复生成。

示例：

- `order-delay:{schedule_id}:{work_order_id}`
- `due-soon:{schedule_id}:{work_order_id}`
- `resource-bottleneck:{schedule_id}:{work_center_id}:{machine_id}`
- `operation-blocking:{schedule_id}:{operation_id}`
- `external-risk:{schedule_id}:{work_order_id}:{operation_id}`

注意：

- `issue_key` 不使用中文。
- 不使用可变文案生成。
- 不使用排序号生成。
- 同一个方案、同一个问题多次刷新应生成相同 key。

### 为什么不落库完整风险结果

风险结果由排产方案动态计算，完整落库会带来：

- 数据重复。
- 方案更新后风险过期。
- 风险文案变更后历史数据难维护。
- 后续规则升级时难以统一。

因此第一版只保存人工处理状态，风险内容每次由后端聚合计算。

### 数据库迁移策略

当前项目使用 MySQL 8.0 + Alembic 作为唯一 schema 变更入口，不再依赖 `create_all`。

第三阶段实现时建议：

- 在 `backend/app/models/production.py` 新增模型。
- 在 `backend/app/models/__init__.py` 确保模型被导入。
- 使用 `alembic revision` 生成迁移脚本。
- 执行 `alembic upgrade head` 应用迁移。

## 后端 API 规划

接口继续挂在现有 production 路由下。

### GET /api/production/management-dashboard

用途：

- 返回经营问题看板数据。

查询参数：

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| schedule_id | int | 最新方案 | 排产方案 |
| days | int | 30 | 看未来多少天 |
| severity | str | 空 | `high`、`medium`、`low` |
| risk_type | str | 空 | 风险类型 |
| customer | str | 空 | 客户模糊筛选 |
| status | str | 空 | 处理状态 |

返回结构建议：

```json
{
  "schedule": {
    "id": 1,
    "schedule_no": "PS-20260429-001",
    "name": "Production Schedule",
    "status": "active"
  },
  "window": {
    "start_date": "2026-04-29",
    "end_date": "2026-05-29",
    "days": 30
  },
  "summary": {
    "high_risk_count": 3,
    "delayed_order_count": 2,
    "predicted_delay_count": 4,
    "bottleneck_resource_count": 2,
    "external_risk_count": 1,
    "open_issue_count": 5
  },
  "urgent_issues": [],
  "order_risks": [],
  "resource_bottlenecks": [],
  "operation_blocks": [],
  "external_risks": [],
  "issue_states": []
}
```

### 风险问题通用字段

不同类型问题可共享以下字段：

```json
{
  "issue_key": "order-delay:1:12",
  "risk_type": "order_delay",
  "severity": "high",
  "title": "订单 WO-001 预计延期 3 天",
  "description": "焊接资源负荷较高，导致关键工序等待或排队。",
  "suggestion": "建议调整订单优先级、增加该工段班次、临时外协或检查是否存在设备空闲未利用。",
  "work_order_id": 12,
  "order_no": "WO-001",
  "customer_name": "客户A",
  "due_date": "2026-05-10T00:00:00",
  "planned_end_time": "2026-05-13T17:00:00",
  "delay_days": 3,
  "work_center_name": "焊接",
  "machine_name": "焊机1",
  "status": "open",
  "note": ""
}
```

### PATCH /api/production/management-dashboard/issue-state

用途：

- 保存风险问题处理状态和备注。

请求体：

```json
{
  "schedule_id": 1,
  "issue_key": "order-delay:1:12",
  "status": "processing",
  "note": "已联系车间确认焊接加班。"
}
```

校验：

- `status` 必须为 `open`、`processing`、`resolved`、`paused`。
- `issue_key` 必填。
- `schedule_id` 必须存在。

行为：

- 若 `schedule_id + issue_key` 已存在，则更新。
- 若不存在，则创建。
- 返回更新后的状态记录。

### GET /api/production/management-dashboard/export

用途：

- 导出经营风险报告 Excel。

参数：

- 与 `GET /api/production/management-dashboard` 一致。

响应：

- `StreamingResponse`
- Content-Type 使用 Excel MIME。
- 文件名：`经营问题看板_{方案编号}_{日期}.xlsx`

### 后端服务建议

新增：

- `backend/app/services/management_dashboard_service.py`

职责：

- 解析方案。
- 加载排产项。
- 复用订单总览、资源负荷、风险分析逻辑。
- 生成风险问题。
- 合并人工处理状态。
- 应用筛选条件。
- 生成导出 Excel。

新增或扩展 schema：

- `ManagementDashboardResponse`
- `ManagementDashboardSummary`
- `ManagementIssueRow`
- `ManagementIssueStateUpdate`
- `ManagementIssueStateRead`

避免：

- 前端重新聚合订单完工时间。
- 前端重新计算资源负荷。
- 前端拼接风险原因。

## 前端页面规划

### 新增页面

建议新增：

- `frontend/src/pages/ManagementDashboard.jsx`

建议路由：

- `/management-dashboard`

建议导航文案：

- `经营问题看板`

页面定位：

- 不是大屏。
- 不是 KPI 总览。
- 是老板和调度员看的问题工作台。

### 新增前端 API

在 `frontend/src/api/production.js` 增加：

- `getManagementDashboard(params)`
- `updateManagementIssueState(payload)`
- `exportManagementDashboard(params)`

### 页面结构

#### 顶部筛选区

筛选项：

- 排产方案。
- 时间范围，默认 30 天。
- 风险等级。
- 问题类型。
- 客户。
- 处理状态。

操作：

- 刷新。
- 导出 Excel。

#### 摘要区

展示：

- 高风险订单数。
- 预计延期订单数。
- 已延期订单数。
- 瓶颈资源数。
- 外协风险数。
- 未处理问题数。

摘要卡片要求：

- 使用简洁中文。
- 高风险使用红色。
- 中风险使用橙色。
- 已处理使用绿色。
- 不使用花哨动画。

#### 最紧急问题清单

默认第一屏主体显示最紧急问题。

字段：

- 风险等级。
- 问题类型。
- 订单号。
- 客户。
- 交期。
- 预计完成。
- 问题原因。
- 建议动作。
- 处理状态。
- 备注。

操作：

- 查看订单。
- 查看排班表。
- 修改状态。
- 编辑备注。

#### 分区列表

下方分区：

- 订单交付风险。
- 资源瓶颈。
- 关键工序阻塞。
- 外协风险。
- 问题处理记录。

分区要求：

- 默认不要一次性展开过多内容。
- 支持折叠或分页。
- 保持表格可读性。
- 不让页面挤成一团。

### 跳转关系

从问题看板可跳转：

- 订单详情：`/scheduling/orders/:workOrderId?schedule_id=...`
- 指定方案生产排班表：`/scheduling/board/:scheduleId`
- 甘特图：`/gantt?schedule_id=...`
- 订单完工表：`/schedule-results?schedule_id=...`
- 派工与工时：`/dispatch?schedule_id=...`

跳转要求：

- 风险问题属于哪个 `schedule_id`，下钻就必须带哪个方案。
- 经营问题看板、订单完工表、派工、生产排班表、甘特图之间互跳时不丢当前方案。

### 中文文案

页面不得出现：

- Loading
- Error
- Search
- Submit
- Status
- Operation
- Machine
- Resource

应使用：

- 加载中
- 加载失败
- 搜索
- 提交
- 状态
- 工序
- 设备
- 资源

## Excel 导出规划

### 文件名

`经营问题看板_{方案编号}_{日期}.xlsx`

示例：

`经营问题看板_PS-20260429-001_20260429.xlsx`

### Sheet 1：老板摘要

字段：

- 排产方案。
- 统计范围。
- 高风险问题数。
- 已延期订单数。
- 预计延期订单数。
- 瓶颈资源数。
- 外协风险数。
- 未处理问题数。
- 最严重问题。
- 建议优先动作。

### Sheet 2：订单交付风险

字段：

- 风险等级。
- 处理状态。
- 订单号。
- 客户。
- 产品。
- 数量。
- 交期。
- 预计完成。
- 延期天数。
- 瓶颈资源。
- 原因。
- 建议动作。
- 备注。

### Sheet 3：瓶颈资源

字段：

- 风险等级。
- 处理状态。
- 工段。
- 设备。
- 负荷率。
- 占用分钟。
- 可用分钟。
- 影响订单数。
- 影响延期订单数。
- 原因。
- 建议动作。
- 备注。

### Sheet 4：关键工序阻塞

字段：

- 风险等级。
- 处理状态。
- 订单号。
- 客户。
- 零件。
- 工序。
- 工段。
- 设备。
- 计划开始。
- 计划结束。
- 后续受影响工序数。
- 原因。
- 建议动作。
- 备注。

### Sheet 5：外协风险

字段：

- 风险等级。
- 处理状态。
- 订单号。
- 客户。
- 外协工段。
- 零件。
- 工序。
- 预计开始。
- 预计完成。
- 是否最后完成工序。
- 是否导致延期。
- 原因。
- 建议动作。
- 备注。

### Sheet 6：问题处理记录

字段：

- 问题编号。
- 问题类型。
- 风险等级。
- 标题。
- 处理状态。
- 备注。
- 更新时间。

### 导出边界

第三阶段第一版已导出：

- 看板摘要。
- 订单交付风险。
- 资源瓶颈。
- 关键工序阻塞。
- 外协风险。
- 全部问题状态。

第三阶段暂不导出：

- 物料风险。
- 人员绩效。
- 成本分析。
- PDF 报告。

物料作为后续阶段扩展。

## 延展性与维护性设计

### 风险计算与人工状态分离

第三阶段应坚持：

- 风险内容动态计算。
- 人工状态单独保存。
- 不落库完整风险快照。

好处：

- 排产方案变化后风险自动变化。
- 风险规则升级后可重新计算。
- 人工处理状态仍能保留。
- 避免重复存储过期数据。

### 风险规则模块化

建议把风险生成拆成独立函数：

- `build_order_delay_issues`
- `build_due_soon_issues`
- `build_resource_bottleneck_issues`
- `build_operation_blocking_issues`
- `build_external_risk_issues`

每类问题独立生成统一结构，方便后续增加：

- 物料风险。
- 模具风险。
- 执行异常。
- 质量返工。
- 成本超标。

### 前端组件可复用

建议拆出轻量组件：

- 风险摘要卡片。
- 问题列表。
- 问题状态选择。
- 备注编辑。
- 风险等级标签。

避免：

- 把所有逻辑堆在一个大页面。
- 前端重复计算风险。
- 每个分区写一套完全不同的表格。

### 后续物料风险扩展

第三阶段不做物料字段，但后续可扩展：

- 订单级物料状态。
- 物料需求表。
- 原材料库存。
- 缺口数量。
- 影响订单。
- 最早影响日期。

未来物料风险加入看板时，应作为新风险类型：

- `material_risk`

不应推翻第三阶段的风险问题结构。

### 后续执行反馈扩展

后续执行反馈可扩展：

- 谁做了。
- 实际开始。
- 实际结束。
- 实际耗时。
- 完成结果。
- 异常原因。

执行反馈加入后，看板可新增：

- 已开工但超时。
- 已完工但影响后续。
- 异常暂停。
- 返工风险。

### 后续权限扩展

第一版只做已有登录基础。

后续可扩展：

- 老板可查看全部问题。
- 调度员可处理问题。
- 查看者只读。

第三阶段第一版不强制增加复杂权限。

## 第三阶段可跟踪清单

| 模块 | 目标 | 关键内容 | 是否阻塞三阶段 | 状态 |
| --- | --- | --- | --- | --- |
| 经营问题看板接口 | 后端返回老板风险视图 | 摘要、问题清单、分区风险 | 是 | 已实现，待真实数据验收 |
| 风险问题状态表 | 保存处理状态和备注 | issue_key、status、note | 是 | 已实现，迁移版本 `202605060001` |
| 风险规则生成 | 识别订单、资源、工序、外协风险 | 高中低风险、建议动作 | 是 | 已实现，外协口径已修 |
| 看板页面 | 老板一眼看交付风险 | 默认未来 30 天、问题清单 | 是 | 已实现，待真实数据验收 |
| 问题状态维护 | 调度员记录处理进度 | 未处理、处理中、已处理、暂缓 | 是 | 已实现，待保存回显验收 |
| Excel 导出 | 导出经营风险报告 | 6 个 Sheet | 是 | 已实现，文件名已修 |
| 跳转下钻 | 从问题定位到订单和排班 | 订单详情、排班表、甘特图 | 否 | 已实现，待页面回归 |
| 筛选能力 | 缩小问题范围 | 方案、客户、风险等级、状态 | 否 | 已实现，待真实数据验收 |
| 物料风险延展 | 后续扩展预留 | 暂不实现字段和约束 | 否 | 暂缓 |

## 当前进度记录（2026-05-05）

### 已完成

- 新增 `business_risk_issue_states` 模型和 Alembic 迁移。
- 新增经营问题看板后端服务，基于 `production_*` 动态计算风险。
- 新增经营看板 API：
  - `GET /api/production/management-dashboard`
  - `PATCH /api/production/management-dashboard/issue-state`
  - `GET /api/production/management-dashboard/export`
- 新增前端 `/management-dashboard` 页面。
- 接入导航、筛选、摘要卡、问题清单、状态备注编辑、跳转下钻、Excel 导出。
- 更新 `README.md` 中三阶段状态、接口、页面、迁移和验收流程。
- 修复外协风险等级口径：延期为 high、临近交期为 medium、普通最后外协不生成风险。
- 修复导出文件名：API 和前端统一为 `经营问题看板_{方案编号}_{日期}.xlsx`。
- 新增真实 FUBEI 工艺表验收数据脚本，读取 `焊接件明细`，生成 5 张派生订单 Excel。
- 脚本可准备基础数据和工序映射，并可直接导入 5 单、运行排产、导出经营问题看板 Excel。

### 已验证

- `C:\Users\48295\.conda\envs\aps\python.exe -m compileall backend\app backend\alembic` 通过。
- `npm run build` 通过。
- `alembic current` 在第三阶段单独验收时为 `202605060001`；当前全量主线 head 为 `202605190001`。
- 后端应用可导入，并能列出三条经营看板路由。
- 真实 FUBEI 派生验收数据导入预览通过：每单 64 个零件、153 道工序、35 道外协工序、0 个错误。
- 自动导入 5 单并生成排产方案 `PS-20260505-080000-000000`。
- 经营问题看板命中五类风险：`order_delay`、`due_soon`、`resource_bottleneck`、`operation_blocking`、`external_risk`。
- 状态备注保存后回读成功，经营问题看板 Excel 已导出并可打开，包含 6 个 Sheet。
- 订单排产时间表已导出并可打开，包含订单级时间表和 765 条工序排产明细。

### Review findings（已修复）

1. ~~外协风险等级口径偏重。~~ **已修复**
   - 修复前：只要外协工序是最后完成工序，就可能被判为高风险。
   - 修复后：外协延期 → `high`；外协临近交期 → `medium`；普通最后完成不生成风险。

2. ~~导出文件名口径不一致。~~ **已修复**
   - 修复前：API 响应头固定为 `management_dashboard.xlsx`，前端下载名也缺少日期。
   - 修复后：`export_management_dashboard_to_excel` 返回 `(bytes, filename)` 元组，API 用 `urllib.parse.quote` 编码后写入 `Content-Disposition`，前端从响应头解析文件名。

### 下一步优先级

先跑通数据闭环，再做 UI 打磨：

1. ~~修复外协风险等级口径。~~ ✓
2. ~~修复经营风险导出文件名。~~ ✓
3. ~~准备真实排产方案，覆盖订单延期、临近交期、资源瓶颈、关键工序阻塞、外协影响。~~ ✓
4. 后续用前端手工复核筛选、跳转订单详情、生产排班表和甘特图。
5. 前端手工复核通过后，再进入 UI 重构和老接口收敛。

## 当前代码复核补充（2026-05-17）

- 后端 `app` 目录通过 `python -m compileall app`。
- 前端通过 `npm run build`。
- 经营问题看板第一版代码完成度仍与本计划一致：接口、页面、状态保存、下钻链接、Excel 导出均已实现。
- 仍缺正式自动化测试套件，下一步应优先把真实 FUBEI 验收数据流固化为可重复测试或脚本。
- 旧 demo API 仍保留在代码中，后续需要明确兼容边界，避免和 `production_*` 主线混用。

## Test Plan

### 文档检查

- `PLAN1.md` 和 `plan2.md` 不被修改。
- 根目录新增 `plan3.md`。
- 文档明确第三阶段承接一阶段、二阶段。
- 文档明确第三阶段默认未来 30 天。
- 文档明确物料暂不实现字段和约束。
- 文档明确轻量问题处理状态和备注。
- 文档包含数据库、API、前端、导出、风险规则。
- 文档不规划与 `production_*` 主线冲突的新表。
- 文档语言为中文，能直接作为后续开发依据。

### 后续实现验收

- 后端可以返回经营问题看板数据。
- 前端可以展示老板交付风险视图。
- 默认展示未来 30 天风险。
- 风险问题可以更新状态和备注。
- 导出 Excel 多 Sheet 可以打开。
- 原订单总览、甘特图、排班表、订单详情不被破坏。
- 页面默认展示问题清单，不是默认图表大屏。
- 风险问题能跳转到订单详情或排班视图。
- 已处理问题可以筛选隐藏或单独查看。

### 回归检查

- `python -m compileall backend/app` 通过。
- `npm run build` 通过。
- 登录后原页面仍可访问。
- `/schedule-results` 仍显示订单级排产总览。
- `/schedule-results`、`/dispatch`、`/gantt` 有 `schedule_id` 时显示指定方案，无 `schedule_id` 时默认最新方案。
- `/scheduling/board` 可从侧边栏进入生产排班表并默认打开最新方案。
- `/scheduling/board/:scheduleId` 仍显示指定方案生产排班表。
- `/scheduling/orders/:workOrderId` 仍显示订单排产详情。
- 结果类页面顶部显示当前方案编号、创建时间、订单数和延期数。

## Assumptions

- 本次只规划并新建 `plan3.md`。
- 物料风险不进入第三阶段实现，仅作为后续扩展方向。
- 风险问题由后端动态计算，人工状态单独保存，避免重复存储过期分析结果。
- 第三阶段看板以问题清单为主，不做炫技大屏。
- 默认时间范围为未来 30 天。
- 问题处理第一版只做状态和备注，不做负责人、截止时间和多条处理日志。
- 导出第一版只做 Excel，不做 PDF。
- 技术栈保持 FastAPI、SQLAlchemy、MySQL 8.0、Alembic、React、Vite、Axios、纯 CSS。
