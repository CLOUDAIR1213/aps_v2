# APS v2 核心基础数据落地路线文档计划

## Summary

本文档用于持续记录 APS v2 当前阶段的真实落地目标：**先把核心基础数据打牢，让系统稳定知道每个订单什么时候完工，并支持后续导出排产结果**。

本次更新保留原计划框架，在现有内容基础上补充一阶段可跟踪清单、UI 打磨要求、数据治理规则和验收状态表，方便后续按模块推进。

当前确认的产品决策：
- 第一阶段只做核心基础数据：工段、设备、人员、资源组、工序映射、订单导入模板稳定。
- 执行反馈后置，不作为当前优先开发内容。
- 录入方式采用“页面维护 + Excel 模板导入”。
- 工序映射采用“自动匹配 + 人工确认”。
- 基础数据编码必须手填，便于对齐企业现有编号体系。
- Excel 重复导入时，相同编码更新已有数据。
- 被历史排产引用的数据不物理删除，只允许禁用。
- 资源组第一版只做建档、分类、筛选，不进入排产算法。
- 排产导出第一版使用一个 Excel 文件，包含四张表：订单总览、生产排班表、资源负荷、逾期风险。

当前一阶段目标：
- 先把工段、设备、人员、资源组、工序映射、订单导入模板做扎实。
- 不急着做执行反馈、复杂日历、库存、模具。
- 一阶段验收重点是：能稳定导入、能稳定匹配资源、能稳定排出订单预计完工时间。
- 人员第一阶段只作为基础档案和后续执行记录对象，不作为复杂排产约束；后续重点记录谁做了、实际什么时候做、实际用了多久、结果如何。

## Key Changes

### 文档新增内容

- 写清楚落地优先级：
  - 核心基础数据
  - 订单级排产结果
  - 排产结果导出
  - 轻量日历
  - 标准工艺建模
  - 执行反馈

- 记录核心判断：
  - 排产不是第一步，建数据才是第一步。
  - 当前不追求复杂日历、库存、模具、现场反馈。
  - 当前最重要的是让计划员看到每张订单预计什么时候完工，并能把结果导出。

### 第一阶段详细需求

- 工段管理：
  - 必填编码、名称。
  - 是否外协。
  - 默认日产能分钟。
  - 默认外协周期小时。
  - 状态：启用、禁用。

- 设备管理：
  - 必填设备编码、设备名称、所属工段。
  - 日产能分钟。
  - 状态：启用、禁用、维修、停机。
  - 第一版不做维修计划和复杂能力参数。

- 人员管理：
  - 必填工号、姓名、所属工段。
  - 状态：在职、停用。
  - 第一版不做技能等级、班组、班次。
  - 人员当前不用于复杂排产优化，只用于标记工段归属、排班显示和后续执行反馈。
  - 后续执行反馈中，人员负责记录每道工序的实际执行人、实际开始、实际结束、实际耗时和执行结果。

- 资源组管理：
  - 资源组编码、名称、说明。
  - 可关联工段、设备、人员（member_type 校验为 work_center/machine/personnel，且 member_id 必须存在）。
  - 第一版只用于分类、筛选、后续扩展，不参与排产约束。

- 工序映射：
  - Excel 工序列名通过 `operation_mapping_rules` 表匹配系统工段。
  - 未匹配项在导入预览中标记为错误，默认阻塞确认入库。
  - 工单导入页支持用户显式选择“自动添加缺失映射”，系统会为缺失工序列创建同名工段和映射规则；空图号、NO 缺失等其他错误仍阻塞入库。
  - 用户在工序映射页创建映射规则后，预览自动识别为已映射。
  - 仅 status=active 的映射规则参与预览和导入。
  - 支持选择已有工段或就地新建工段后保存映射。
  - 第一版不做复杂相似度算法，先做名称标准化和人工确认。

- 订单导入模板稳定：
  - 继续优先解析 `焊接件明细`。
  - 保持现有空图号、非数字工时、缺资源映射提示。
  - 明确导入前检查项：订单号、客户、产品、数量、优先级、交期。
  - 不强制企业马上换统一新模板，先以校验和映射提升稳定性。

### 一阶段可跟踪清单

#### 工段管理

- 编码必须手填，页面维护和 Excel 导入都要保证唯一。
- 名称必填，用于工序映射规则匹配。
- 是否外协必填，外协工段参与周期约束但不占内部设备。
- 默认日产能分钟必填，第一版默认 480 分钟。
- 默认外协周期小时必填，作为外协无工时时的兜底周期。
- 状态支持：启用、禁用。
- 禁用工段后：其下 pending 工序不参与排产；若存在启用工序依赖禁用工段工序，排产在创建方案前阻塞并报错，不会留下空方案。
- 风险检查：内部工段没有启用设备时，标记为阻塞问题。
- 验收标准：每个内部工段至少能看到设备数量、启用设备数和总日产能。

#### 设备管理

- 设备编码必须手填，且全局唯一。
- 设备名称必填。
- 所属工段必填。
- 日产能分钟必填，作为资源负荷和后续日历计算基础。
- 状态支持：启用、禁用、维修、停机。
- 排产时仅 status=active 的设备参与候选；禁用、维修、停机设备不参与排产。
- 工段下全部设备非 active 时，排产阻塞并提示"没有启用的设备"。
- 第一版不做维修计划、保养周期、复杂加工能力参数。
- 风险检查：工段下没有启用设备时提示阻塞。
- 验收标准：设备能按工段分组展示，并能看出每个工段的实际可用产能。

#### 人员管理

- 工号必须手填，且全局唯一。
- 姓名必填。
- 所属工段必填。
- 状态支持：在职、停用。
- Excel 导入兼容 `机台人员` / `机组人员` 工作表；表格按第 2 行工段名称、第 3 行 `工号` / `姓名` 子表头解析，第 4 行起为人员数据。
- 外协列使用 `外协` 标记时只跳过人员导入，不创建人员和内部工段关联。
- 第一版不做技能等级、班组、班次、考勤和工资。
- 第一版不把人员作为严格产能约束；排产仍以工段和设备为主。
- 人员档案的主要作用是给后续执行记录提供“谁做了这项工序”的可选对象。
- 后续工序执行记录需要关联人员，并记录实际开始时间、实际结束时间、实际用时、完成结果和备注。
- 风险检查：工段没有在职人员时提示风险，但不阻塞排产。
- 验收标准：人员能按工段分组展示，生产排班表可以显示默认人员或未分配；后续报工时可以选择执行人员。

#### 人员执行记录定位

- 人员不是第一阶段的复杂约束对象，而是后续执行反馈的数据主体。
- 每条工序执行记录应回答：
  - 谁做了这道工序。
  - 实际什么时候开始。
  - 实际什么时候完成。
  - 实际用了多久。
  - 完成结果如何。
  - 是否有异常、返工、暂停或备注。
- 计划时间来自排产结果，实际时间来自现场记录。
- 计划工时和实际工时要能对比，用于后续分析工时偏差、人员效率和工艺工时准确性。
- 第一阶段只先把人员档案建好，执行记录模型和页面放到后续阶段。

#### 资源组管理

- 资源组编码必填。
- 资源组名称必填。
- 说明字段用于记录用途，例如“焊接组”“龙门加工组”“外协组”。
- 可关联工段、设备、人员。
- 第一版只用于分类、筛选、配置管理和后续扩展。
- 第一版不进入排产算法，不改变当前 `WorkCenter -> ResourceMachine` 的排产逻辑。
- 验收标准：资源组能作为基础配置中心中的筛选维度。

#### 工序映射

- Excel 工序列名自动匹配系统工段。
- 未匹配项进入人工确认，不直接静默导入。
- 支持选择已有工段。
- 支持确认创建新工段。
- 支持标记外协。
- 保存映射规则，后续导入复用。
- 第一版不做复杂相似度算法，先做名称标准化和人工确认。
- 风险检查：未匹配工序列会在导入预览和数据健康检查中提示。
- 验收标准：常见工序列如 `下料`、`拼装`、`焊接`、`5m龙门`、`气攻`、`钣金外` 能稳定映射。

#### 订单导入模板稳定

- 继续优先解析 `焊接件明细`。
- 检查订单号、客户、产品、数量、优先级、交期。
- 检查空图号、空 NO、非数字工时、缺失资源映射。
- 预览阶段不写库。
- 确认后再生成工单、零件、工序和依赖。
- 不强制企业马上换统一新模板，先通过校验和映射降低导入失败率。
- 验收标准：导入前能清楚看到新增任务数量、工时汇总、阻塞问题和提醒问题。

### 一阶段 UI 打磨要求

#### 基础配置中心

- 作为统一入口，不只是普通菜单。
- 展示工段、设备、人员、映射、导入模板的准备状态。
- 每个模块都显示：已配置数量、主要问题、处理入口。
- 目标是让计划员进入页面后知道“现在能不能排产，还缺什么数据”。

#### 数据准备度看板

- 显示排产准备度百分比。
- 状态分为：可排产、有风险、不建议排产。
- 统计无设备工段、无人员工段、未匹配工序、停用设备。
- 阻塞问题影响排产按钮提示；风险问题只提醒，不阻止排产。

#### 工序映射确认页

- 左侧显示 Excel 工序列。
- 右侧显示系统工段。
- 显示匹配状态：已匹配、需确认、外协、未配置。
- 提供修改、新建工段、标记外协操作。
- 保存后形成映射规则，下次导入自动复用。

#### 设备管理 UI

- 按工段分组展示设备。
- 每个工段显示设备数、启用设备数、总日产能、风险提示。
- 设备列表显示编码、名称、状态、日产能分钟。
- 工段无启用设备时用明显阻塞标识。

#### 人员管理 UI

- 按工段分组展示人员。
- 花名册每一名人员行都显示自己的关联工段，不只在分组第一行显示。
- 支持页面维护和 Excel 导入。
- 显示无人员工段风险。
- 停用人员不参与后续派工和人员视图。
- 页面文案避免像人事系统，重点表达“执行人员”“所属工段”“状态”。
- 后续报工页面中，人员用于选择实际执行人，而不是在第一阶段参与复杂排产计算。

#### 基础数据导入预览

- 上传 Excel 后先解析预览。
- 预览显示新增、更新、跳过、异常。
- 相同编码更新已有数据。
- 缺少必填编码、名称、所属工段等字段时标记为阻塞。
- 用户确认后写入数据库。

#### 数据健康检查

- 统一展示阻塞、风险、提醒。
- 每条问题显示：严重程度、问题、影响、处理入口。
- 示例：
  - 阻塞：工段“焊接”没有启用设备，焊接工序无法排产。
  - 风险：工段“气攻”没有人员，排班表人员显示不完整。
  - 提醒：设备“龙门-02”已停用，不参与排产。

#### 六步向导

1. 工段配置。
2. 设备配置。
3. 人员配置。
4. 工序映射。
5. 导入模板检查。
6. 运行排产。

每一步显示状态：未开始、进行中、已完成、有风险。

### 一阶段跟踪表

| 模块 | 目标 | 关键字段/能力 | UI 入口 | 是否阻塞排产 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| 工段管理 | 建清楚系统有哪些生产/外协工段 | 编码、名称、是否外协、默认产能、状态 | 资源配置页 | 内部工段无设备时阻塞 | 已完成 |
| 设备管理 | 建清楚每个工段有哪些可用设备 | 设备编码、名称、所属工段、日产能、状态 | 资源配置页（展开设备） | 无启用设备时阻塞 | 已完成 |
| 人员管理 | 建清楚人员属于哪个工段，为后续记录谁执行了工序做准备 | 工号、姓名、所属工段、状态 | 人员档案页 | 不阻塞，只提示风险 | 已完成 |
| 资源组管理 | 支持分类、筛选和后续规则扩展 | 资源组编码、名称、关联资源 | 资源分组页 | 不阻塞 | 已完成 |
| 工序映射 | 让 Excel 工序列稳定匹配系统工段 | Excel 列名、系统工段、外协标识、确认状态 | 工序映射页 | 未匹配工序阻塞导入 | 已完成 |
| 订单导入模板 | 稳定解析工艺表并生成任务 | 订单信息、零件、工序、工时、异常预览 | 工单导入页 | 阻塞错误不允许入库 | 已完成 |
| 数据健康检查 | 统一展示基础数据问题 | 阻塞、风险、提醒、处理入口 | 各页面内嵌提示 | 阻塞项影响排产准备度 | 部分完成 |
| 排产结果导出 | 后续导出排产结果用于汇报和车间执行 | 订单总览、排班表、资源负荷、逾期风险 | 订单级排产总览 | 不阻塞排产 | 暂缓 |

### 数据治理规则

- 基础数据编码必须手填。
- 页面维护和 Excel 导入都要遵守编码唯一。
- Excel 重复导入时，同编码更新已有数据。
- 历史排产引用过的数据禁用不删除。
- 禁用数据默认不参与后续排产和筛选推荐。
- 旧 demo 代码已删除，旧数据库表不作为新主线继续扩展。
- 当前主线继续以 `production_*` 模型为准。

### 后续阶段规划

- 订单级排产结果：
  - 每张订单预计开始、预计完成、延期天数、主要瓶颈。
  - 点击订单下钻到零件和工序解释。
  - 默认不展示所有工序堆叠。

- 排产结果导出：
  - 新增导出目标：`排产结果_{方案编号}.xlsx`。
  - Sheet 1：订单排产总览。
  - Sheet 2：生产排班表。
  - Sheet 3：资源负荷。
  - Sheet 4：逾期风险。

- 轻量日历：
  - 后续再做班次、设备停机、加班、人员日历。
  - 当前仍沿用固定班制。

- 标准工艺建模：
  - 后续再做货品品目、标准工艺路线、产品版本。
  - 当前仍以工单 Excel 带工艺为主。

- 执行反馈：
  - 后续再做今日派工、开工、完工、异常、剩余任务重排。
  - 执行反馈的核心不是复杂 MES，而是记录每道工序谁做了、实际开始、实际结束、实际耗时和结果。
  - 已完成、异常、暂停等状态后续再影响剩余任务重排。
  - 当前不进入第一阶段开发目标。

## Test Plan

文档完成后检查：

- 是否明确写出“第一阶段先打基础数据，不急执行反馈”。
- 是否明确写出“当前目标是看清每个订单什么时候完工，并能导出结果”。
- 是否覆盖 6 个核心基础数据模块：工段、设备、人员、资源组、工序映射、订单导入模板。
- 是否记录编码规则：必须手填。
- 是否记录重复导入规则：相同编码更新已有数据。
- 是否记录删除策略：禁用不删除。
- 是否记录资源组第一版不进入排产算法。
- 是否没有把旧 demo 表或旧 demo 接口作为新主线。
- 是否和当前 `production_*` 主线一致。
- 是否明确记录基础配置中心、准备度看板、工序映射确认、导入预览、数据健康检查。
- 是否新增一阶段跟踪表，并能看到每个模块的验收状态。
- 是否记录“阻塞”和“风险”的区别。
- 是否明确人员第一阶段不是复杂排产约束，而是后续执行记录对象。
- 是否明确后续人员执行反馈要记录实际执行人、实际开始、实际结束、实际耗时和执行结果。

## Assumptions

- 本次只更新 `PLAN1.md`，不改代码。
- README 暂不拉长；后续如需要，只增加一个链接入口。
- 当前第一阶段不做日历约束、物料库存、模具刀具、执行反馈。
- 当前第一阶段人员只做基础档案，不做技能矩阵、考勤、工资和人员负荷优化。
- 当前第一阶段仍保持现有技术栈和数据库主线。
- 文档更新方式为保留原计划并增量补充，不推翻原计划。
