# APS v2 第二阶段排产能力与经营问题看板规划

## Summary

本文档用于记录 APS v2 在 `PLAN1.md` 一阶段核心基础数据之后的第二阶段规划。

第二阶段不替代一阶段基础数据计划，而是承接一阶段成果：当工段、设备、人员、资源组、工序映射、订单导入模板稳定后，系统开始重点打磨“可控排产、订单完工解释、结果导出、管理问题识别”。

第二阶段核心目标：

- 计划员可以选择订单和排产开始日期运行排产。
- 系统能稳定回答每张订单什么时候开始、什么时候完成、是否延期、为什么延期。
- 支持整张订单锁定，避免重排反复打乱已经确认的计划。
- 支持导出排产结果 Excel。
- 为下一阶段“经营看板”预留数据口径，看的是问题在哪，不是单纯 KPI。

当前阶段的关键判断：

- 排产建立在基础数据之上，不再回到旧 Demo 主线。
- 当前最重要的是让计划员和管理层看懂订单交付风险。
- 第二阶段不追求数学最优解，先追求可解释、可复盘、可导出、可落地。
- 经营看板后续要围绕问题清单，而不是展示一堆孤立指标。

## 第二阶段定位

第二阶段的定位是：把系统从“已经能排产”提升到“计划员能使用、管理层能判断问题”的排产系统。

第二阶段先解决：

- 哪些订单可以排。
- 每张订单排到什么时候。
- 哪些订单可能延期。
- 为什么延期。
- 哪些资源是瓶颈。
- 结果如何导出给老板、计划员、车间。

第二阶段暂不作为主线：

- 不做复杂 MES。
- 不急着做人员排班约束。
- 不急着做库存齐套硬约束。
- 不急着做模具、刀具、工装约束。
- 不急着做复杂节假日、加班审批、设备维修审批。
- 不急着做现场开工、完工、异常反馈闭环。

人员在第二阶段仍保持当前定位：

- 人员不作为复杂排产优化约束。
- 人员可以作为工段基础档案、排班显示、后续执行记录对象。
- 后续执行反馈再记录谁做了、实际什么时候做、实际用了多久、结果如何。

## 第二阶段核心目标

### 目标 1：可控排产

计划员不应该只能点击“全部待排一键排”。第二阶段需要支持计划员明确选择：

- 从哪一天开始排。
- 排哪些订单。
- 是否基于已有方案重排。
- 哪些订单已经确认不能动。

当前实现口径：

- 排产驾驶台展示可排订单，包含 `pending` 和 `scheduled` 工序对应的订单。
- 首次排产后，已排订单仍可被选择并基于历史方案重排。
- 排产失败时不创建空排产方案，不污染历史方案列表。

### 目标 2：订单完工解释

系统要优先回答订单级问题：

- 订单预计开始时间。
- 订单预计完成时间。
- 是否超过交期。
- 延期几天。
- 最后完成的是哪个零件、哪道工序。
- 可能卡在哪个工段或设备。

默认展示层级继续保持：

1. 订单级总览。
2. 点击订单查看零件级时间线。
3. 继续下钻查看工序级明细和依赖关系。

### 目标 3：结果稳定可导出

第二阶段要让排产结果能被拿出去使用：

- 给老板看订单交付风险。
- 给计划员看设备排班。
- 给车间看近期任务。
- 给后续复盘看资源瓶颈和延期原因。

### 目标 4：为经营问题看板打底

第二阶段不需要一次性做完整经营看板，但要让数据结构和分析口径能支撑下一阶段：

- 订单风险。
- 资源瓶颈。
- 物料风险。
- 工序阻塞。
- 外协周期风险。

## 排产能力升级

### 排产方式

第二阶段从“全部待排一键排”升级为“选择订单排产”。

这里的可选订单包含：

- `pending`：尚未排产的订单。
- `scheduled`：已经排过、允许基于历史方案重新计算的订单。

排产运行参数至少包括：

- 排产开始日期。
- 参与排产的订单范围。

可后续扩展的排产参数：

- 基于哪个历史方案重排。
- 是否保留已锁订单。
- 是否只排未排订单。
- 排产时间范围。
- 排序规则：优先级优先、交期优先、客户优先。

第一版默认排序规则继续保持：

1. 优先级高的订单优先。
2. 交期早的订单优先。
3. 导入时间早的订单优先。
4. 必要时工时短的工序优先。

### 技术主线

第二阶段继续使用现有 `production_*` 主线：

- `WorkCenter`
- `ResourceMachine`
- `Personnel`
- `WorkCenterPersonnel`
- `WorkOrder`
- `ImportBatch`
- `Part`
- `ProductionOperation`
- `OperationDependency`
- `ProductionSchedule`
- `ProductionScheduleItem`

不新增与当前含义冲突的重复表。

### 班制规则

继续遵守固定班制：

- 周一到周六：`08:00-12:00`、`13:00-17:00`。
- 周日休息。
- 任务不能安排在午休。
- 任务不能安排在下班后。
- 跨天任务自动顺延。

暂不增加：

- 法定节假日。
- 加班审批。
- 设备维修审批。
- 复杂多班制。

### 排产规则

保留现有规则：

- 工序依赖不能被破坏。
- 同一零件内部工序按顺序执行。
- 子件任务可并行进入资源队列。
- 上级部件拼装、焊接等任务必须等待下级子件准备完成。
- 同一设备任务不能重叠。
- 外协工序参与时间约束，但不占内部设备。
- Excel 工序数字按单件工时入库，排产时按 `ProductionOperation.duration_hours * Part.quantity` 折算产能工时。

第二阶段增强点：

- 支持按订单范围排产。
- 支持按指定开始日期排产。
- 支持已锁订单占用资源，未锁订单避让。
- 支持生成新的历史方案，不覆盖旧方案。
- 排产成功的新方案状态为 `active`；历史 `draft` 方案不批量回填。

## 订单锁定与重排策略

用户侧锁定粒度：整张订单锁定。

锁定的业务含义：

- 这张订单的计划已经确认。
- 这张订单可能已经导出或沟通过。
- 后续重排时不希望它被随意改变。

重排规则：

- 已锁订单重排时不改变计划。
- 本次勾选的未锁订单允许重新排。
- 未勾选且未锁订单不会自动进入新方案。
- 每次重排仍生成新的历史方案。
- 新方案需要继承已锁订单的计划占用。
- 未锁订单不得与已锁订单在同一设备上重叠。
- 若重排过程中发现无启用设备、依赖环或缺失依赖，系统应报错并且不新增空方案。

建议技术实现：

- 用户在界面上看到的是“锁定订单”。
- 后端可以通过锁定该订单在某个方案下的所有 `ProductionScheduleItem` 实现。
- 后续如需更细控制，可扩展为工序级锁定，但第二阶段不暴露给用户。

锁定状态建议：

- 未锁定：可参与重排。
- 已锁定：重排时保留。
- 已导出：建议默认提示是否锁定。

## 外协周期规则

第二阶段外协规则采用轻量方式：

- Excel 中有工时时，优先使用 Excel 工时。
- Excel 中没有工时时，使用工段配置的默认外协周期。
- 外协工序参与订单完工时间计算。
- 外协不占用内部设备。
- 同一外协工段可作为一个外协时间通道参与排程。

暂不做：

- 供应商日历。
- 供应商产能。
- 外协报价。
- 外协采购单。
- 外协发出、收回反馈。

后续可扩展：

- 外协供应商档案。
- 外协默认交期。
- 外协在途状态。
- 外协超期风险。
- 外协成本分析。

## 排产结果导出

第二阶段导出目标是让结果能用于汇报、分发和复盘。

导出形式：

- 一个 Excel 文件。
- 使用系统数据生成，不回写原始 `.xlsm` 工艺表。

至少包含四张表：

### 订单完工表

用于老板、销售、计划员看订单交付。

字段建议：

- 排产方案号。
- 订单号。
- 客户。
- 产品名称。
- 数量。
- 优先级。
- 交期。
- 预计开始。
- 预计完成。
- 延期天数。
- 状态。
- 主要瓶颈。

### 设备排班表

用于车间和计划员查看每天做什么。

字段建议：

- 日期。
- 工段。
- 设备。
- 订单号。
- 客户。
- 零件图号。
- 零件名称。
- 工序。
- 计划开始。
- 计划结束。
- 工时。
- 是否外协。

### 资源负荷表

用于判断哪些资源忙、哪些资源空。

字段建议：

- 工段。
- 设备。
- 占用分钟。
- 可用分钟。
- 负荷率。
- 状态：瓶颈、正常、空闲较多。

### 延期风险表

用于管理层查看交付风险。

字段建议：

- 订单号。
- 客户。
- 交期。
- 预计完成。
- 延期天数。
- 瓶颈资源。
- 原因。
- 建议动作。

导出按钮位置：

- 订单完工表页。
- 排产方案选择区附近。
- 导出前明确当前导出的是哪个排产方案。

当前前端口径：

- `/schedule-results` 页面显示为“订单完工表”。
- URL 有 `schedule_id` 时导出该方案；URL 没有时默认最新方案。
- 从订单完工表进入派工、生产排班表、甘特图时必须保留当前方案。

## 排产结果解释

第二阶段的结果解释重点不是展示更多数据，而是把关键问题说清楚。

订单详情页需要继续强化：

- 订单摘要。
- 零件级时间线。
- 工序级排产明细。
- 工序依赖关系。
- 最后完成工序。
- 主要瓶颈资源。

延期解释第一版规则：

1. 如果订单预计完成时间晚于交期，则标记延期。
2. 找出该订单中结束最晚的工序。
3. 取该工序所在工段作为瓶颈候选。
4. 如果该工段或设备负荷率较高，则标记为可能瓶颈。
5. 给出中文原因和建议。

建议文案：

- 原因：`{工段名称}资源负荷较高，导致关键工序等待或排队。`
- 建议：`建议调整订单优先级、增加该工段班次、临时外协或检查是否存在设备空闲未利用。`

## 经营问题看板延展规划

下一阶段的经营看板不是看 KPI，而是看问题在哪。

管理层关心的不是单纯数字，而是：

- 哪些订单快延期了。
- 哪些设备用得过重。
- 哪些设备明显闲置。
- 哪张订单用到的原料快断了。
- 哪个工序卡住了后面全部流程。

### 看板定位

经营看板应当是“问题清单 + 影响范围 + 建议动作”。

不建议做成：

- 大屏炫技。
- 只展示漂亮指标。
- 只显示总订单数、总工时、平均负荷率。

建议默认展示：

- 今日最需要处理的订单风险。
- 本周最明显的瓶颈资源。
- 即将影响交付的物料风险。
- 卡住后续流程的关键工序。
- 外协周期异常任务。

### 预留问题类型

#### 订单交付问题

系统需要识别：

- 已延期订单。
- 预计延期订单。
- 距离交期很近但仍有关键工序未完成的订单。
- 交期承诺明显超出当前产能的订单。

输出字段建议：

- 问题类型。
- 影响订单。
- 客户。
- 交期。
- 预计完成。
- 延期天数或风险天数。
- 主要原因。
- 建议动作。

#### 设备负荷问题

系统需要识别：

- 负荷率过高的设备。
- 长时间排满的设备。
- 明显空闲的设备。
- 同工段内设备负荷不均衡。

输出字段建议：

- 工段。
- 设备。
- 负荷率。
- 忙闲状态。
- 影响订单数。
- 影响工序数。
- 建议动作。

#### 物料风险问题

第二阶段暂不实现库存约束，但要为下一阶段预留口径。

未来系统需要识别：

- 哪张订单用到的原料快断了。
- 哪些零件因为物料不到位不能开工。
- 哪些物料影响多个订单。
- 哪些物料需要优先采购。

输出字段建议：

- 物料编号。
- 物料名称。
- 当前库存。
- 需求数量。
- 缺口数量。
- 影响订单。
- 最早影响日期。
- 建议动作。

#### 工序阻塞问题

系统需要识别：

- 哪个工序卡住了后面全部流程。
- 哪道工序是当前订单关键路径。
- 哪个工段等待时间最长。
- 哪个零件拖慢了整张订单。

输出字段建议：

- 订单号。
- 零件。
- 工序。
- 工段。
- 计划开始。
- 计划结束。
- 后续受影响工序数。
- 后续受影响订单或部件。
- 建议动作。

#### 外协周期问题

系统需要识别：

- 外协周期过长的任务。
- 外协工序导致订单延期的任务。
- 外协任务集中在某个工段或供应商的风险。

输出字段建议：

- 外协工段。
- 订单号。
- 零件。
- 工序。
- 预计发出。
- 预计完成。
- 影响交期。
- 建议动作。

### 看板交互建议

经营看板默认显示问题，不默认显示全部明细。

建议交互：

- 按问题严重程度排序。
- 支持筛选：订单、客户、工段、设备、交期范围。
- 点击问题下钻到订单详情、设备排班、工序明细或物料明细。
- 每条问题显示影响和处理入口。

## 下一代延展性设计

第二阶段开发时要为后续扩展留空间，但不提前把复杂功能全部做进去。

### 轻量日历

后续可扩展：

- 设备停机。
- 加班。
- 特殊休息日。
- 人员上班日历。

当前策略：

- 第二阶段继续用固定班制。
- 不引入复杂日历系统。
- 后端班制计算函数保持可替换。

### 物料齐套

后续可扩展：

- 原材料档案。
- 原材料库存。
- 订单物料需求。
- 缺料风险。
- 不齐套任务标记为不可排或有风险。

当前策略：

- 第二阶段先记录经营看板需要的物料风险口径。
- 不把库存作为硬约束加入排产。

### 工具和模具约束

后续可扩展：

- 工具类型。
- 模具编号。
- 适用工序。
- 模具占用。
- 模具冲突。

当前策略：

- 第二阶段暂不加入排产算法。
- 未来可作为 `ProductionOperation` 的额外约束参与资源选择。

### 执行反馈

后续可扩展：

- 谁做了。
- 实际什么时候开始。
- 实际什么时候结束。
- 实际用了多久。
- 结果如何。
- 是否返工。
- 是否暂停。
- 异常原因。

当前策略：

- 第二阶段不做执行反馈闭环。
- 人员数据继续作为未来执行反馈对象。

### 滚动重排

后续可扩展：

- 根据现场反馈只重排未完成任务。
- 已完成任务不再改变。
- 已开工任务可选择锁定。
- 未开工任务根据新状态滚动重排。

当前策略：

- 第二阶段先做订单锁定和选择订单排产。
- 为未来滚动重排打基础。

## 第二阶段可跟踪清单

| 模块 | 目标 | 关键内容 | 是否阻塞第二阶段 | 状态 |
| --- | --- | --- | --- | --- |
| 选择订单排产 | 计划员可控运行排产 | 开始日期、订单范围，支持待排/已排订单 | 是 | 已完成 |
| 工时口径 | 产能占用不低估 | Excel 单件工时 × 零件数量，前端显示产能工时 | 是 | 已完成 |
| 排产开始日期 | 支持从指定日期排 | 班制校正到工作时间 | 是 | 已完成 |
| 整单锁定 | 保护已确认计划 | 锁定订单、重排保留 | 是 | 已完成 |
| 重排策略 | 不覆盖旧方案 | 新方案只包含本次勾选订单和已锁订单，未锁订单避让，失败不留空方案 | 是 | 已完成 |
| 外协周期 | 外协影响完工时间 | 工时优先，否则默认周期 | 是 | 已完成 |
| 订单完工解释 | 让计划员看懂结果 | 开始、完成、延期、瓶颈 | 是 | 已完成 |
| Excel 导出 | 结果可汇报可分发 | 四张核心表 | 是 | 已完成 |
| 资源负荷解释 | 判断忙闲和瓶颈 | 负荷率、状态、影响订单 | 否 | 已完成 |
| 延期风险解释 | 说明为什么延期 | 最晚工序、瓶颈资源、建议 | 否 | 已完成 |
| 经营问题看板口径 | 为下一阶段预留 | 订单、设备、工序、外协问题；物料风险暂缓 | 否 | 已落地到 PLAN3 第一版 |

## Test Plan

文档检查：

- 仓库根目录存在 `plan2.md`。
- `PLAN1.md` 原内容不被修改。
- `plan2.md` 明确承接一阶段基础数据。
- `plan2.md` 明确第二阶段重点是可控排产、订单完工解释、锁单、导出。
- `plan2.md` 明确执行反馈、库存、模具、复杂日历暂不作为第二阶段主线。
- `plan2.md` 包含经营问题看板延展规划。
- `plan2.md` 不规划与当前 `production_*` 主线冲突的新表。
- 文档语言为简洁中文，便于后续直接跟踪开发。

后续实现验收：

- 在 MySQL 中先创建 `aps_v2` database，再进入 conda 环境 `aps` 执行 `python -m alembic.config upgrade head`。
- 执行 `python -m alembic.config current`，第二阶段单独验收至少应包含 `202605050001`；当前全量主线 head 为 `202605190001`。
- 计划员能选择订单和开始日期运行排产。
- 首次排产后，已排订单仍能在排产驾驶台被选择并基于历史方案重排。
- Excel 工序数字按单件工时入库，排产占用按 `ProductionOperation.duration_hours * Part.quantity` 折算。
- 已锁订单重排后计划不变。
- 未锁订单能避让已锁订单资源占用。
- 基于历史方案重排时，未勾选且未锁订单不会进入新方案。
- 无启用设备、依赖环或缺失依赖导致排产失败时，不新增空 `production_schedules` 记录。
- 排产成功的新方案 `status` 为 `active`。
- 订单总览能显示预计开始、预计完成、延期天数、瓶颈资源。
- 排产结果能导出 Excel。
- 导出 Excel 包含订单完工表、设备排班表、资源负荷表、延期风险表。
- 经营问题看板的下一阶段数据口径清晰，不需要推翻第二阶段设计。

## Assumptions

- 文件名使用 `plan2.md`。
- 本文档只规划第二阶段，不修改 `PLAN1.md`。
- 第二阶段不推翻一阶段基础数据计划。
- 第二阶段继续沿用 `production_*` 主线。
- 第二阶段不新建与当前含义冲突的重复表。
- 第二阶段不完整实现经营看板，只为下一阶段预留问题识别口径。
- 经营看板的核心不是 KPI，而是帮助管理层快速判断问题在哪。
