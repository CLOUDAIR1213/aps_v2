# APS v2 前端 UI 风格与页面逻辑重构规划

## Summary

本文档用于规划 APS v2 前端 UI 和页面逻辑重构。目标不是做炫酷大屏，也不是单纯换颜色，而是把系统整理成 **简约、清晰、每一步逻辑明确的小企业 APS 工作台**。

本规划承接：

- `PLAN1.md`：核心基础数据。
- `plan2.md`：可控排产、锁单、导出。
- `plan3.md`：老板交付风险看板。

重构方向：

- 页面更简洁，减少装饰。
- 用户清楚知道当前在哪一步、下一步做什么。
- 一阶段基础数据填写流程更明确。
- 二阶段排产操作更可控。
- 三阶段经营问题看板更直接。
- 保留现有功能，分阶段重构，不一次性推翻。

核心原则：

- 前端负责展示流程、状态、操作入口。
- 后端负责排产、延期、负荷、瓶颈、风险等聚合计算。
- 不让前端重新计算订单完工、资源负荷和经营风险。
- UI 优先服务计划员和老板的判断，不做营销式界面。

## 前端重构目标

当前前端已经具备 APS v2 原型能力，但仍有几个问题：

- 页面组织仍偏 Demo 页面集合。
- `App.jsx` 承担导航、页面说明、快捷操作、流程步骤和登录壳，职责过多。
- `ScheduleResults.jsx` 同时承担订单总览、运行排产、风险列表、资源负荷和工序明细，页面过重。
- `Scheduling.jsx` 仍偏“全部队列一键排”，和二阶段“选择订单排产”目标不完全一致。
- `styles.css` 持续膨胀，后续加入基础配置中心和经营看板会更难维护。
- 旧 Demo 页面与 APS v2 主线混在一起，用户容易分不清当前系统主流程。

重构目标：

- 把页面按真实业务流程组织。
- 把基础数据、排产、结果、经营问题分层。
- 让用户每一步都知道当前状态和下一步动作。
- 统一简约清晰的工业管理系统风格。
- 拆分可复用组件，减少大页面和大 CSS 文件继续膨胀。

## UI 风格定位

整体风格调整为：

- 简约。
- 清晰。
- 工业管理系统风格。
- 低饱和灰白底。
- 少量蓝绿色作为主色。
- 红色只用于延期、高风险、阻塞。
- 橙色只用于预警。
- 绿色只用于完成、正常、可排产。
- 不使用花哨渐变、大面积阴影、复杂动画。
- 不做营销式首页。
- 不做炫技大屏。

视觉重点：

- 信息层级清楚。
- 表格可扫读。
- 操作按钮位置稳定。
- 当前步骤突出。
- 异常和风险一眼能看到。
- 页面不要堆太多卡片。

### 颜色规则

建议颜色口径：

- 页面背景：浅灰。
- 主内容区：白色或极浅灰。
- 主色：低饱和蓝绿。
- 信息色：蓝灰。
- 正常：绿色。
- 预警：橙色。
- 延期、阻塞、高风险：红色。
- 禁用、弱信息：灰色。

颜色使用原则：

- 状态颜色只表达业务状态，不做装饰。
- 一页中不出现过多强调色。
- 高风险和阻塞必须比普通提醒更醒目。
- 按钮不使用大面积鲜艳色，避免干扰判断。

### 版式规则

- 使用稳定的左侧导航 + 顶部页面标题 + 主工作区。
- 页面首屏优先展示当前阶段最重要的信息。
- 表格和工具条优先，卡片适量。
- 卡片用于摘要，不用于堆叠所有内容。
- 表格列要紧凑但可读。
- 操作按钮靠近相关数据区域。
- 重要页面要提供空状态、加载状态、失败状态。

## 页面逻辑主线

前端主流程按用户真实工作顺序重排：

1. 基础配置。
2. 工单导入。
3. 数据检查。
4. 运行排产。
5. 查看订单结果。
6. 查看排班表。
7. 查看经营问题。

每个页面都要回答三个问题：

- 当前是什么步骤。
- 当前还有什么问题。
- 下一步应该点哪里。

页面顶部应保持统一：

- 页面名称。
- 当前步骤说明。
- 关键操作按钮。
- 当前方案或当前数据状态。

避免：

- 页面标题过长。
- 首屏出现大量解释性文字。
- 操作入口分散。
- 用户不知道下一步应该做什么。

## 一阶段基础数据 UI 规划

一阶段基础数据 UI 是本次重构的重点之一。基础数据不是普通表单，而是排产能否运行的前置条件。

建议新增或改造为：

- `基础配置中心`

页面定位：

- 数据准备工作台。
- 告诉用户现在能不能排产。
- 告诉用户缺哪些基础数据。
- 给出处理入口。

### 基础配置中心核心模块

基础配置中心应包含：

- 排产准备度。
- 六步配置进度。
- 阻塞问题。
- 风险提醒。
- 工段配置。
- 设备配置。
- 人员配置。
- 工序映射。
- 导入模板检查。
- 数据健康检查。

### 六步向导

六步向导：

1. 工段配置。
2. 设备配置。
3. 人员配置。
4. 工序映射。
5. 工单导入检查。
6. 运行排产。

每一步显示状态：

- 未开始。
- 进行中。
- 已完成。
- 有风险。
- 阻塞。

每一步显示：

- 当前完成情况。
- 主要问题。
- 处理入口。
- 是否阻塞排产。

### 工段配置 UI

页面目标：

- 建清楚系统有哪些内部工段和外协工段。
- 让 Excel 工序列能稳定映射到系统工段。

字段：

- 工段编码。
- 工段名称。
- 是否外协。
- 默认日产能分钟。
- 默认外协周期小时。
- 状态。

UI 逻辑：

- 工段是第一步。
- 编码必须手填。
- 名称必填。
- 内部工段后续必须有设备。
- 外协工段不要求设备。
- 外协工段显示默认周期。

风险提示：

- 内部工段没有设备：阻塞。
- 工段名称和 Excel 工序列不一致：风险。
- 外协工段没有默认周期：风险或阻塞，取决于是否允许 Excel 工时兜底。

### 设备配置 UI

页面目标：

- 让用户清楚每个内部工段有多少可用设备和日产能。

展示方式：

- 按工段分组。
- 每个工段显示设备数量、启用设备数、总日产能。
- 工段无启用设备时显示阻塞。

字段：

- 设备编码。
- 设备名称。
- 所属工段。
- 日产能分钟。
- 状态：启用、禁用、维修、停机。

UI 逻辑：

- 内部工段必须至少有一台启用设备。
- 停用、维修、停机设备不参与排产。
- 状态变化要影响数据健康检查。

### 人员配置 UI

页面目标：

- 建清楚人员属于哪个工段。
- 为后续执行记录“谁做了这道工序”做准备。

字段：

- 工号。
- 姓名。
- 所属工段。
- 状态。

UI 逻辑：

- 人员不作为当前排产硬约束。
- 工段无人员只提示风险，不阻塞排产。
- 支持页面维护和 Excel 导入。
- 停用人员不参与后续派工和人员视图。

页面文案：

- 使用“执行人员”“所属工段”“状态”。
- 避免做成人事系统。

### 工序映射 UI

页面目标：

- 让 Excel 工序列稳定匹配系统工段。

推荐布局：

- 左侧：Excel 工序列。
- 右侧：系统工段。
- 中间或状态列：匹配状态。

状态：

- 已匹配。
- 需确认。
- 外协。
- 未配置。

操作：

- 选择已有工段。
- 创建新工段。
- 标记外协。
- 保存映射规则。

UI 逻辑：

- 未匹配项不能静默导入。
- 用户确认后后续导入复用。
- 工序映射问题应同步出现在导入预览和数据健康检查。

### 工单导入模板检查 UI

页面目标：

- 让用户在入库前知道 Excel 能不能用于排产。

上传前提示：

- 订单号。
- 客户。
- 产品名称。
- 数量。
- 优先级。
- 交期。

上传后显示：

- 新增数量。
- 更新数量。
- 跳过数量。
- 异常数量。
- 零件数量。
- 工序数量。
- 总工时。
- 涉及工段。

UI 逻辑：

- 预览阶段不写库。
- 阻塞错误不允许确认入库。
- 提醒问题允许继续，但要明确影响。
- 确认入库后提示下一步进入排产驾驶台。

### 数据健康检查 UI

数据健康检查按三类展示：

- 阻塞。
- 风险。
- 提醒。

每条问题显示：

- 严重程度。
- 问题描述。
- 影响范围。
- 处理入口。

示例：

- 阻塞：工段“焊接”没有启用设备，焊接工序无法排产。
- 风险：工段“气攻”没有人员，排班表人员显示不完整。
- 提醒：设备“龙门-02”已停用，不参与排产。

## 二阶段排产 UI 规划

### 排产驾驶台

`/scheduling` 从“全部队列一键排”升级为“可控排产驾驶台”。

页面结构：

- 排产范围：选择订单。
- 排产开始日期。
- 待排订单摘要。
- 阻塞问题提示。
- 执行排产按钮。
- 最新方案入口。

排产按钮附近必须清楚显示：

- 排哪些订单。
- 从哪天开始排。
- 是否存在阻塞问题。
- 排产后会生成新方案，不覆盖旧方案。

交互逻辑：

- 默认显示待排订单。
- 支持按客户、交期、优先级筛选。
- 支持全选、反选、清空。
- 没有选中订单时不能运行排产。
- 存在阻塞问题时提示原因。

### 订单排产总览

`/schedule-results` 改为订单级结果页。

页面重点：

- 默认只看订单。
- 每张订单显示预计开始、预计完成、延期天数、瓶颈。
- 点击订单再看零件和工序。
- 工序明细不要默认压在第一屏。

保留入口：

- 甘特图。
- 生产排班表。
- 资源负荷。
- 逾期风险。
- 导出。
- 锁单。

### 生产排班表

`/scheduling/board/:scheduleId` 保留为车间排班表。

优化方向：

- 顶部筛选更紧凑。
- 日期矩阵保持横向滚动。
- 周日和休息日灰底。
- 外协和延期标识明显。
- 任务信息列固定。

## 三阶段经营问题看板 UI 规划

新增：

- `/management-dashboard`

页面定位：

- 老板看交付风险。
- 调度员看问题处理状态。
- 默认不是图表，而是问题清单。

第一屏显示：

- 高风险订单数。
- 已延期订单数。
- 预计延期订单数。
- 瓶颈资源数。
- 外协风险数。
- 未处理问题数。
- 最紧急问题清单。

问题列表字段：

- 风险等级。
- 问题类型。
- 订单号。
- 客户。
- 交期。
- 预计完成。
- 原因。
- 建议动作。
- 处理状态。
- 备注。

交互：

- 修改处理状态。
- 编辑备注。
- 跳转订单详情。
- 跳转排班表。
- 导出 Excel。

页面要求：

- 第一屏直接显示问题。
- 不做全屏大屏。
- 不用复杂图表替代问题清单。
- 高风险和未处理问题必须醒目。

## 组件拆分规划

建议新增组件目录。

### components/layout

- `MainLayout`
- `SidebarNav`
- `PageHeader`
- `StepIndicator`

职责：

- 页面框架。
- 导航。
- 页面标题。
- 当前流程步骤。

### components/common

- `StatusBadge`
- `SummaryCards`
- `FilterBar`
- `DataState`
- `SectionPanel`

职责：

- 通用状态。
- 摘要卡。
- 筛选条。
- 加载、空、失败状态。
- 通用分区容器。

### components/master-data

- `SetupProgress`
- `DataHealthPanel`
- `WorkCenterSetupTable`
- `MachineSetupTable`
- `PersonnelSetupTable`
- `OperationMappingTable`

职责：

- 一阶段基础数据准备。
- 数据健康检查。
- 工段、设备、人员、映射维护展示。

### components/scheduling

- `ScheduleRunForm`
- `OrderGanttChart`
- `RiskOrderTable`
- `ResourceLoadPanel`
- `ScheduleResultLinks`

职责：

- 二阶段可控排产。
- 订单级结果。
- 资源负荷。
- 结果入口。

### components/management

- `IssueSummaryCards`
- `IssueList`
- `IssueStatusEditor`
- `ManagementRiskTable`

职责：

- 三阶段经营问题看板。
- 风险问题列表。
- 状态和备注维护。

组件拆分目标：

- 页面负责组织业务流程。
- 组件负责展示。
- API 聚合计算仍由后端完成。
- 不让前端重新计算延期、负荷、瓶颈。

## 样式体系规划

继续使用纯 CSS，不引入重 UI 库。

建议整理为：

- `layout.css`
- `forms.css`
- `tables.css`
- `status.css`
- `master-data.css`
- `scheduling.css`
- `board.css`
- `management-dashboard.css`

如果暂时不拆文件，也要在 `styles.css` 内按模块分区。

统一风格规则：

- 页面背景：浅灰。
- 主内容：白底或极浅灰。
- 边框：浅灰细线。
- 圆角：控制在 6-8px。
- 阴影：少用，弱化。
- 表格优先，卡片适量。
- 字号紧凑但可读。
- 状态色统一。
- 操作按钮不使用大面积鲜艳色。
- 危险动作和风险提示必须醒目。

### 状态样式

统一状态：

- 阻塞：红色。
- 高风险：红色。
- 预警：橙色。
- 正常：绿色。
- 信息：蓝灰。
- 禁用：灰色。

### 表格样式

表格要求：

- 行高稳定。
- 表头清楚。
- 数字右对齐或统一格式。
- 关键字段加粗。
- 状态列使用标签。
- 操作列位置固定。

### 表单样式

表单要求：

- 标签明确。
- 必填项明显。
- 错误提示靠近字段。
- 主要按钮固定在表单末尾。
- 不把所有字段塞进一行。

## 路由与菜单规划

菜单按业务流程重排。

### 数据准备

- 基础配置中心。
- 工单导入。
- 工序映射。

### 排产执行

- 排产驾驶台。
- 生产排班表。

### 结果分析

- 订单排产总览。
- 甘特图。
- 资源负荷。
- 逾期风险。

### 经营问题

- 经营问题看板。

### 系统基础

- 设备管理。
- 人员管理。
- 旧 Demo 入口折叠或弱化。

旧 Demo 页面不立即删除，但不作为主导航重点。

## 分阶段实施顺序

### 第一步：前端骨架重构

- 拆 `App.jsx`。
- 抽离导航配置。
- 抽离页面 meta。
- 建立统一 `MainLayout`。
- 保持现有路由可用。

### 第二步：UI 风格统一

- 收敛颜色、间距、按钮、表格、状态标签。
- 去掉过重装饰。
- 保持简约清晰。
- 优化页面首屏信息密度。

### 第三步：基础数据 UI 重构

- 新增基础配置中心。
- 增加六步向导。
- 增加数据准备度。
- 增加健康检查。
- 优化工段、设备、人员、工序映射入口。

### 第四步：排产 UI 重构

- `/scheduling` 支持选择订单和开始日期。
- `/schedule-results` 聚焦订单级结果。
- 工序级明细下沉。
- 加入锁单和导出入口预留。

### 第五步：经营问题看板接入

- 新增 `/management-dashboard`。
- 默认问题清单。
- 支持状态、备注、导出。
- 保持老板视角清晰。

## Test Plan

文档完成后检查：

- 是否明确 UI 风格为简约、清晰、步骤明确。
- 是否覆盖一阶段基础数据填写逻辑。
- 是否包含基础配置中心和六步向导。
- 是否明确阻塞、风险、提醒的区别。
- 是否没有把人员当作当前排产硬约束。
- 是否明确二阶段排产页面逻辑。
- 是否明确三阶段经营问题看板页面逻辑。
- 是否明确组件拆分和样式整理方向。
- 是否保持 React + Vite + Axios + 纯 CSS。
- 是否不要求一次性推翻现有页面。

后续实现验收：

- 原有页面仍可访问。
- 新菜单按业务流程组织。
- 用户能从基础数据一路走到排产结果。
- 基础配置中心能显示准备度和问题。
- 排产驾驶台能说明当前排哪些订单、从哪天开始。
- 订单结果页默认只展示订单级结果。
- 经营问题看板默认展示问题清单。
- 页面中文文案简洁专业。
- `npm run build` 通过。

## Assumptions

- 本次只规划 `plan-ui-refactor.md`。
- 不立即改代码。
- 前端重构分阶段进行。
- 旧 Demo 页面可以弱化，但不立即删除。
- 新 UI 以 APS v2 `production_*` 主线为准。
- 前端不承担复杂排产聚合计算，订单完工、延期、负荷、瓶颈仍由后端返回。
