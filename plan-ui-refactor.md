# APS v2 UI 规划与管理文档

## 1. UI 总目标

本文档是 APS v2 唯一的 UI 规划和管理文档。后续前端页面、样式、原型和验收口径都以本文为准；其他文档如需提到 UI，只引用本文，不再单独扩写另一套 UI 规范。

本文既记录目标风格，也记录当前前端落地状态；不要再把 `ui-mockups/README.md` 当作 UI 规划来源。静态 mockup 只保留为历史原型资料。

APS v2 的 UI 目标是做成 **简约、清晰、表格优先的生产管理后台**，服务对象是计划员、生产管理人员和老板。界面重点不是展示炫技效果，而是让用户快速完成工单导入、排产、派工、排班复核、外协跟踪和交付风险判断。

设计原则：

- 业务流程清楚：用户知道当前在哪个模块、正在处理什么数据、下一步该做什么。
- 表格可扫读：订单、工序、人员、排班、风险等核心信息优先用表格表达。
- 操作位置稳定：查询、重置、主操作、行操作在各页面保持一致。
- 状态表达克制：颜色只表达业务状态，不作为装饰。
- 页面密度适中：适合计划员重复操作，避免营销式首页、大屏式 KPI 堆叠和复杂动画。

当前真实状态：

- 前端是 React + Vite，主业务路由集中在 `frontend/src/router.jsx` 和 `frontend/src/navigation.js`。
- 当前已具备计划员工作台、基础数据、工单导入、排产驾驶台、订单完工表、派工、生产排班表、甘特图、外协任务、交付风险看板等页面入口。
- 当前方案已经贯穿订单完工表、派工、生产排班表、甘特图和交付风险看板。
- 人员只用于派工、负荷统计和后续执行记录；内部工段没有 active 人员不阻塞排产。
- 旧 demo 页面、API 封装和后端旧接口代码不再作为当前系统入口；静态 mockup 截图只作为历史原型资料。

当前 UI 落地状态（2026-06-04）：

- 全局壳层已改为浅色生产管理后台风格：`PageHeader.jsx` 提供顶部生产模块栏，`styles.css` 统一浅色侧栏、顶部模块导航、蓝色选中态和紧凑按钮。
- 排产计划页 `Scheduling.jsx` 已作为样板页落地：包含页面二级 tab、订单号/排产状态筛选、开始日期、历史方案、右上角“智能排产”和待排订单表格。
- 首页、订单完工表、派工与工时、生产排班表、工单导入、基础配置、外协任务、甘特图、交付风险看板、工段/设备、人员、工序映射和资源组页面已完成第一轮紧凑后台化。
- 新增 `frontend/src/components/common/CompactSummaryStrip.jsx`，页面级摘要已从大 KPI 卡片收敛为紧凑摘要条；页面层不再直接使用旧 `SummaryCards`。
- 页面层旧 `form-grid` 已清理，主要查询、筛选、新增和导入表单统一使用工具条或紧凑表单样式。
- `frontend/src/styles.css` 仍是主要样式承载文件，尚未按 `layout.css`、`tables.css`、`forms.css`、`status.css`、`pages.css` 拆分。
- 已执行 `npm run build` 并通过；尚未做逐页截图级视觉 QA，因此不能把视觉验收视为完全完成。

## 2. 参考图风格基准

当前 UI 风格以用户提供的简约生产管理后台截图为基准。核心视觉特征如下：

- 浅色左侧分组导航，背景为极浅灰或白色。
- 顶部白色模块导航，承载生产业务一级模块。
- 页面内使用二级 tab 切换同一模块下的视图。
- 查询区为单行紧凑工具条，紧贴表格上方。
- 主内容以白底表格为核心，不堆叠大量卡片。
- 主按钮使用蓝色，选中态使用浅蓝底和蓝色文字。
- 状态点、状态标签、勾选框、色块用于表达业务状态。
- 页面留白干净，但不做大面积空洞布局。

明确避免：

- 深色侧栏。
- 大面积渐变。
- 厚重阴影。
- 大圆角装饰卡片。
- 营销式 hero 首页。
- 炫技大屏和过大的 KPI 视觉。
- 为了装饰使用无业务含义的颜色、图形、水印或复杂背景。

颜色规则：

- 页面背景：`#f5f7fa` 一类浅灰。
- 主内容区：白色或极浅灰。
- 主色：低饱和蓝色，用于主操作、当前选中态、关键链接。
- 成功/正常：绿色，只用于完成、正常、可排产。
- 预警：橙色，只用于临近交期、需注意、可继续但有风险。
- 阻塞/延期/高风险：红色，只用于必须处理的问题。
- 弱信息/禁用：灰色。
- 优先级、颜色标记可使用粉、黄、青、蓝等小面积色块，但不能主导页面视觉。

## 3. 全局布局规范

### 3.1 应用壳层

全局采用“左侧分组导航 + 顶部模块导航 + 主工作区”的后台布局。

左侧导航：

- 使用浅色背景。
- 菜单按业务分组，例如基础数据、生产工艺、生产计划、执行跟踪、管理查看。
- 每个菜单项可使用线性图标和中文名称。
- 当前菜单使用浅蓝背景、蓝色文字、轻量边框或左侧强调线。
- 不使用深色侧栏、厚重阴影和高饱和背景。

顶部模块导航：

- 使用白色横栏。
- 可承载工艺路线、排产计划、排产策略、物料管理、资源管理、生产订单、来料计划、制造 BOM、工序模板、生产日历等生产业务模块。
- 当前模块可使用浅灰底、蓝色下划线或标签选中态。
- 右侧放置全局状态或主操作，例如“上次排产时间”“智能排产”。

主工作区：

- 页面内容从顶部 tab、筛选工具条、表格主体依次展开。
- 首屏优先展示可操作数据，不放大段说明文字。
- 页面标题保持短，不占用过多高度。
- 结果类页面顶部可保留当前方案摘要，但摘要必须紧凑。

### 3.2 页面内 tab

同一业务模块下的同级视图使用页面内二级 tab。

排产计划模块示例：

- 待排订单。
- 资源甘特图。
- 订单甘特图。
- 物料需求甘特图。
- 计划任务。

tab 规则：

- tab 位于主内容顶部。
- 当前 tab 使用蓝色文字和底部蓝线。
- tab 不做成大卡片。
- tab 文案短，避免解释性长句。

### 3.3 筛选工具条

筛选工具条用于查询，不作为页面装饰。

布局规则：

- 单行优先，复杂页面可换行但仍保持紧凑。
- 常用筛选靠左，例如订单号、排产状态、工段、人员、日期。
- 查询、重置按钮紧跟筛选项。
- 主操作按钮靠右，例如智能排产、导出、确认入库。
- 输入框、下拉框和按钮高度统一。

文案规则：

- 输入占位符直接说明字段，例如“请输入订单号”。
- 按钮使用“查询”“重置”“导出”“智能排产”等短文案。
- 不在工具条里放业务说明长句。

### 3.4 表格

表格是 APS v2 的主要信息载体。

表格规则：

- 表头使用浅灰底。
- 行高紧凑但可读。
- 数字、日期、状态列保持稳定宽度。
- 行操作放最右侧。
- 风险、状态、优先级使用小标签、状态点或色块表达。
- 可点击操作使用蓝色文字，不使用过多按钮。
- 大表格允许横向滚动，关键列可固定。

表格优先展示结论字段：

- 订单页面优先显示订单号、产品、数量、交期、预计完成、延期状态。
- 排班页面优先显示任务、工段、人员、日期、工时。
- 派工页面优先显示未派工、已派工、人员分摊和负荷。
- 风险页面优先显示问题、影响订单、风险类型、处理状态和入口。

### 3.5 卡片和摘要

卡片只用于摘要和关键状态，不用于堆叠所有内容。

允许使用卡片的场景：

- 当前方案摘要。
- 排产准备度。
- 阻塞问题数量。
- 延期订单数量。
- 外协风险摘要。

不建议使用卡片的场景：

- 把每个订单做成卡片。
- 把所有步骤做成大流程卡。
- 把表格能清楚表达的数据拆成多张卡。

## 4. 页面级规划

### 4.1 排产计划页面样板

排产计划页面是后续 UI 风格样板，优先按参考图口径实现。

页面结构：

- 顶部模块栏显示生产业务模块，当前模块为“排产计划”。
- 页面内 tab 包含待排订单、资源甘特图、订单甘特图、物料需求甘特图、计划任务。
- 右上角显示上次排产时间和“智能排产”主按钮。
- tab 下方是紧凑筛选工具条。
- 主区是待排订单表格。

筛选区字段：

- 订单号输入框，占位符“请输入订单号”。
- 排产状态下拉框。
- 查询按钮。
- 重置按钮。

待排订单表格建议列：

- 订单号。
- 产品编码。
- 数量。
- 需求日期。
- 优先级。
- 排产状态。
- 排产勾选。
- 显示颜色。
- 操作。

交互规则：

- “智能排产”是页面主操作，固定在右上角。
- 行内操作使用蓝色文字，例如“移动”“详情”。
- 排产勾选使用蓝色勾选框。
- 优先级用小标签表达，不放大。
- 显示颜色用小色块表达，不写成大面积背景。

### 4.2 工单导入

目标：让用户在入库前知道 Excel 能不能用于排产。

页面结构：

- 上传区保持简洁。
- 上传后显示解析摘要、异常列表和确认入库操作。
- 解析结果优先用表格展示。

核心展示：

- 新增数量。
- 更新数量。
- 跳过数量。
- 异常数量。
- 零件数量。
- 工序数量。
- 单件工时合计。
- 按零件数量折算后的产能工时。
- 涉及工段。

规则：

- 预览阶段不写库。
- 阻塞错误不允许确认入库。
- 提醒问题允许继续，但要明确影响。
- 确认入库后提示下一步进入排产计划页面。

当前已落地的导入预览布局：

- 预览区采用左侧“解析出的工序任务”表格、右侧三段式辅助面板的结构。
- 右侧面板顺序为“导入校验”“零件任务概览”“导入备注”，先暴露会阻止入库的错误和提醒。
- 左侧工序任务面板会随右侧预览面板总高度拉伸，但高度上限不超过右侧三个面板的合计高度；窄屏下取消该高度同步，改为上下堆叠。
- 左侧工序任务表格和右侧各列表都在面板内部滚动，避免整页出现大块空白或左右高度错位。
- 零件任务概览中的长备注当前只做两行缩略展示，并保留 `title` 显示完整备注，防止备注把右侧产能工时列挤成竖排。

当前限制：

- 以上调整只影响 `WorkOrderImport.jsx` 和 `styles.css` 的预览阶段展示，不改变 Excel 解析、校验、入库和排产业务逻辑。
- 本轮已通过前端构建和 diff 空白检查，尚未做 Playwright 截图级视觉验收。

### 4.3 基础数据

基础数据不是普通资料维护，而是排产能否运行的前置条件。

页面模块：

- 工段与设备配置。
- 人员配置。
- 资源组管理。
- 工序映射。
- 导入模板检查。
- 数据健康检查。

展示规则：

- 优先使用表格维护数据。
- 顶部显示排产准备状态。
- 阻塞、风险、提醒分开展示。
- 内部工段没有 active 人员是派工风险，不是排产阻塞。
- 设备当前更多用于展示和后续扩展，不作为当前主要硬约束说明。
- 外协工段不要求设备和内部人员。

### 4.4 订单完工表

目标：让计划员和老板一眼看到每张订单什么时候完工。

页面结构：

- 顶部紧凑显示当前方案编号、创建时间、订单数、延期数。
- 主体默认展示订单级表格。
- 工序级明细放到订单详情页，不压在首屏。
- 资源负荷概览放在订单表后面。

表格字段：

- 订单号。
- 客户。
- 产品。
- 交期。
- 预计开始。
- 预计完成。
- 延期天数。
- 瓶颈。
- 锁定状态。
- 操作。

规则：

- 默认按风险排序：延期订单、临近交期、高优先级、交期早。
- 延期红色，临近交期橙色，正常低调显示。
- 锁单文案使用“锁定计划 / 取消锁定”。

### 4.5 订单排产详情

目标：解释为什么这个订单排到当前时间。

首屏结论：

- 延期时显示“预计 xx 完工，晚于交期 x 天”。
- 按期时显示“预计按期完成”。

核心信息：

- 最后完成工序。
- 影响交付的关键工序。
- 前置关系。
- 派工状态。
- 跳转到订单完工表、生产排班表和甘特图的入口。

规则：

- 工序明细表增加序号，体现工序顺序。
- 前置关系使用当前页面序号解释。
- 未派工显示黄色提醒。
- 已派工显示人员、占比和计划工时。

### 4.6 派工与工时

目标：让计划员连续处理未派工任务，并看到人员负荷。

页面结构：

- 顶部紧凑显示当前方案、任务总数、未派工数、已派工数、已分摊工时。
- 主表默认未派工优先。
- 右侧或下方展示人员负荷汇总。
- 人员分配编辑器保持紧凑。

规则：

- 人员下拉显示姓名、工号、所属工段。
- 优先推荐当前工段人员。
- 跨工段人员允许保存，但必须提示。
- 保存后定位下一条未派工任务。
- 支持单人 100% 和多人均分。
- 一键分配需要先预览，再确认保存。

### 4.7 生产排班表

目标：按当前方案查看内部工序和外协工序的时间安排。

页面结构：

- 顶部显示当前方案编号、创建时间、订单数、延期数。
- 筛选栏压缩为视图、工段、工单、日期跨度、搜索。
- 日期矩阵保持横向滚动。
- 任务信息列固定。

规则：

- 默认人员视图。
- 工段和设备视图保留为辅助查看。
- 周日和休息日灰底。
- 外协和延期标识明显但不刺眼。
- 单元格显示工时，悬停显示订单、工序、时间段。

### 4.8 甘特图

目标：查看方案时间线，不做炫技图表。

规则：

- 甘特图服务于排程复核。
- 入口从订单完工表、生产排班表和订单详情进入。
- 支持按当前方案展示。
- 时间轴、工段、订单和任务名称要可读。
- 不使用复杂动画或深色大屏风格。

### 4.9 外协任务

目标：跟踪外协工序状态和预计回厂时间。

页面结构：

- 顶部筛选外协状态、供应商、订单、预计回厂时间。
- 主体表格展示外协任务。

表格字段：

- 订单号。
- 零件。
- 工序。
- 供应商。
- 外协状态。
- 送出时间。
- 预计回厂时间。
- 实际回厂时间。
- 备注。
- 操作。

规则：

- 预计回厂时间变化后提示会影响后续内部工序。
- 状态颜色只表达外协进度和风险。

### 4.10 交付风险看板

目标：识别问题，不做老板大屏。

页面结构：

- 默认展示问题清单。
- 顶部只保留必要摘要。
- 风险类型、影响订单、原因、处理状态、处理入口用表格展示。

规则：

- 延期、资源瓶颈、外协风险、未派工风险分开。
- 风险原因由后端返回，前端不重新计算。
- 已处理、暂缓、持续关注等状态使用低调标签。

## 5. 组件与样式规范

### 5.1 组件分层

布局组件：

- 应用壳层。
- 左侧导航。
- 顶部模块栏。
- 页面标题栏。
- 页面 tab。

通用组件：

- 筛选工具条。
- 数据表格。
- 状态标签。
- 状态点。
- 摘要条。
- 空状态。
- 加载状态。
- 失败状态。

业务组件：

- 排产订单表。
- 订单完工表。
- 工序明细表。
- 派工任务表。
- 人员负荷表。
- 生产排班矩阵。
- 外协任务表。
- 风险问题表。

### 5.2 样式文件方向

当前第一轮 UI 落地仍集中在 `frontend/src/styles.css`，这是已实现状态，不是最终样式组织目标。后续样式应逐步从单一大文件收敛为按职责组织：

- `layout.css`：应用壳层、导航、顶部栏。
- `tables.css`：表格、行状态、固定列。
- `forms.css`：筛选工具条、输入框、下拉框、按钮。
- `status.css`：标签、状态点、风险颜色。
- `pages.css`：少量页面级布局。

规则：

- 使用统一 CSS 变量管理颜色、间距、边框、字号。
- 不为单个页面重复定义相同按钮、表格和标签样式。
- 不引入大面积装饰性背景。
- 不使用会影响表格扫读的强阴影和高饱和块。

### 5.3 文案规范

文案要短、业务化、可操作。

推荐：

- 智能排产。
- 查询。
- 重置。
- 锁定计划。
- 取消锁定。
- 查看详情。
- 进入派工。
- 查看排班表。

避免：

- 大段解释性说明压在首屏。
- 把技术字段直接暴露给用户。
- 使用“demo”“mock”“测试页”等历史原型文案。
- 同一操作在不同页面使用不同名称。

## 6. 当前实施状态与后续顺序

以下状态以当前前端代码为准。

已落地：

1. 排产计划页面。
   - 按参考图建立顶部模块栏、页面 tab、筛选工具条和待排订单表格。
   - 保留右上角“智能排产”主按钮。

2. 全局壳层。
   - 左侧导航改为浅色分组导航。
   - 顶部模块栏统一生产业务模块入口。
   - 当前选中态统一为浅蓝底和蓝色文字。

3. 表格和筛选工具条统一。
   - 统一表头、行高、状态标签、行操作。
   - 统一输入框、下拉框、查询、重置、导出、主按钮样式。
   - 页面层旧 `SummaryCards` 和旧 `form-grid` 已清理，摘要条和工具条已覆盖主页面。

4. 订单完工表、派工与工时、生产排班表。
   - 这三类页面是计划员高频页面，应优先套用新风格。
   - 保证当前方案贯穿不丢失。

5. 外协任务、甘特图、交付风险看板。
   - 按表格优先和问题清单优先原则继续收敛。
   - 避免回到大屏化或卡片堆叠风格。

6. 首页和基础数据维护页。
   - 首页、工单导入、基础配置中心、工段/设备、人员、工序映射、资源组已使用紧凑摘要条和工具条风格。

后续仍需：

1. 样式文件拆分。
   - 将 `styles.css` 中的布局、表格、表单、状态和页面级样式逐步拆到独立文件。

2. 逐页视觉 QA。
   - 在真实后端数据下检查桌面和窄屏视口，确认表格、工具条、弹窗和矩阵没有遮挡或溢出。

3. 清理历史 UI 表述。
   - 其他文档如需提到 UI，只链接本文。
   - 不再把 `ui-mockups/README.md` 作为 UI 规划来源。
   - 静态 mockups 只作为历史原型材料，不承担规划职责。

## 7. 验收标准

### 7.1 文档验收

- `plan-ui-refactor.md` 是唯一 UI 规划和管理文档。
- 文档能回答整体 UI 应该是什么风格。
- 文档能说明导航、顶部栏、tab、筛选区、表格如何组织。
- 文档能说明哪些视觉元素应该避免。
- 文档能说明哪些页面优先改造。
- 文档没有要求修改 API、数据库、路由或后端逻辑。

### 7.2 实现验收

当前第一轮实现已经满足大部分结构性验收，但逐页截图级视觉 QA 尚未完成。完整验收应满足：

- 页面整体接近参考图的简约后台风格。
- 左侧导航为浅色分组导航。
- 顶部模块栏为白色横栏。
- 页面内同级视图使用二级 tab。
- 查询区为紧凑工具条。
- 主体数据优先使用表格。
- 卡片只用于摘要和关键状态。
- 蓝色只用于主操作、选中态和关键链接。
- 红、橙、绿只用于业务风险、预警和正常完成状态。
- 首屏不出现大段说明文字、大面积 KPI 卡片或营销式布局。
- 页面中文文案简洁专业。
- `npm run build` 通过。
- 真实数据下页面无明显横向遮挡、工具条挤压、弹窗内容溢出或表格主体不可扫读问题。

## 8. Assumptions

- 本文档既规划 UI，也记录当前 React/CSS 已落地状态。
- 用户提供的截图是当前 UI 风格基准。
- APS v2 的 UI 优先服务计划员的高频生产管理操作。
- 前端不承担复杂排产聚合计算，订单完工、延期、负荷、瓶颈和风险原因仍由后端返回。
- 其他文档可链接本文，但不再维护独立 UI 规范。
