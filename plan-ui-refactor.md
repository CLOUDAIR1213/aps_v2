# APS v2 前端 UI 风格与页面逻辑重构规划

## Summary

本文档用于规划 APS v2 前端 UI 和页面逻辑重构。目标不是做炫酷大屏，也不是单纯换颜色，而是把系统整理成 **简约、清晰、每一步逻辑明确的小企业 APS 工作台**。

本规划承接：

- `PLAN1.md`：核心基础数据。
- `plan2.md`：可控排产、锁单、导出。
- `plan3.md`：老板交付风险看板。

重构方向：

- 页面更简洁，减少装饰。
- 用户清楚知道当前在哪一步、下一步做什么。
- 一阶段基础数据填写流程更明确。
- 二阶段排产操作更可控。
- 三阶段经营问题看板更直接。
- 保留现有功能，分阶段重构，不一次性推翻。

当前落地状态（2026-05-21）：

- 阶段一“统一主流程”已完成：`/` 改为计划员工作台，首页按基础数据、工单、方案状态给出唯一下一步主按钮。
- 阶段二“当前方案贯穿全局”已完成：订单完工表、派工、生产排班表、甘特图、经营问题看板统一围绕一个当前方案跳转和展示。
- 阶段三“排产驾驶台流程优化”已完成：`/scheduling` 改为本次排产设置、订单选择、执行结果三块，排产动作像一个明确命令。
- 阶段四“订单完工表重构”已完成：`/schedule-results` 首屏聚焦每张订单什么时候完工，工序明细默认收起，资源负荷放到订单表后。
- 阶段五“订单详情优化”已完成：`/scheduling/orders/:id` 顶部直接解释为什么排到当前时间，突出最后完成工序和关键工序。
- 本轮没有新增业务模型，没有重写排产系统，只调整前端信息架构、导航、当前方案传递、排产命令、订单完工和订单解释体验。

核心原则：

- 前端负责展示流程、状态、操作入口。
- 后端负责排产、延期、负荷、瓶颈、风险等聚合计算。
- 不让前端重新计算订单完工、资源负荷和经营风险。
- UI 优先服务计划员和老板的判断，不做营销式界面。

## 前端重构目标

当前前端已经具备 APS v2 原型能力，但仍有几个问题：

- 页面组织仍偏 Demo 页面集合。
- `App.jsx` 承担导航、页面说明、快捷操作、流程步骤和登录壳，职责过多。
- `ScheduleResults.jsx` 同时承担订单总览、运行排产、风险列表、资源负荷和工序明细，页面过重。
- `Scheduling.jsx` 仍偏“全部队列一键排”，和二阶段“选择订单排产”目标不完全一致。
- `styles.css` 持续膨胀，后续加入基础配置中心和经营看板会更难维护。
- 旧 Demo 页面与 APS v2 主线混在一起，用户容易分不清当前系统主流程。

重构目标：

- 把页面按真实业务流程组织。
- 把基础数据、排产、结果、经营问题分层。
- 让用户每一步都知道当前状态和下一步动作。
- 统一简约清晰的工业管理系统风格。
- 拆分可复用组件，减少大页面和大 CSS 文件继续膨胀。

## UI 风格定位

整体风格调整为：

- 简约。
- 清晰。
- 工业管理系统风格。
- 低饱和灰白底。
- 少量蓝绿色作为主色。
- 红色只用于延期、高风险、阻塞。
- 橙色只用于预警。
- 绿色只用于完成、正常、可排产。
- 不使用花哨渐变、大面积阴影、复杂动画。
- 不做营销式首页。
- 不做炫技大屏。

视觉重点：

- 信息层级清楚。
- 表格可扫读。
- 操作按钮位置稳定。
- 当前步骤突出。
- 异常和风险一眼能看到。
- 页面不要堆太多卡片。

### 颜色规则

建议颜色口径：

- 页面背景：浅灰。
- 主内容区：白色或极浅灰。
- 主色：低饱和蓝绿。
- 信息色：蓝灰。
- 正常：绿色。
- 预警：橙色。
- 延期、阻塞、高风险：红色。
- 禁用、弱信息：灰色。

颜色使用原则：

- 状态颜色只表达业务状态，不做装饰。
- 一页中不出现过多强调色。
- 高风险和阻塞必须比普通提醒更醒目。
- 按钮不使用大面积鲜艳色，避免干扰判断。

### 版式规则

- 使用稳定的左侧导航 + 顶部页面标题 + 主工作区。
- 页面首屏优先展示当前阶段最重要的信息。
- 表格和工具条优先，卡片适量。
- 卡片用于摘要，不用于堆叠所有内容。
- 表格列要紧凑但可读。
- 操作按钮靠近相关数据区域。
- 重要页面要提供空状态、加载状态、失败状态。

## 页面逻辑主线

前端主流程按用户真实工作顺序重排：

1. 运行总览。
2. 工单导入。
3. 排产驾驶台。
4. 订单完工表。
5. 派工与工时。
6. 生产排班表。
7. 甘特图。
8. 基础配置。

经营问题看板保留，但不作为计划员主导航重点。它从订单结果、甘特图和风险处理入口进入。

每个页面都要回答三个问题：

- 当前是什么步骤。
- 当前还有什么问题。
- 下一步应该点哪里。

页面顶部应保持统一：

- 页面名称。
- 当前步骤说明。
- 关键操作按钮。
- 当前方案或当前数据状态。

当前方案展示规则：

- 结果类页面顶部统一显示当前方案编号、创建时间、订单数、延期数。
- URL 有 `schedule_id` 时，以 URL 为准。
- URL 没有 `schedule_id` 时，默认最新方案。
- `/scheduling/board/:scheduleId` 使用路径中的方案 ID。
- 跨订单完工表、派工、生产排班表、甘特图、经营问题看板跳转时不丢方案。

避免：

- 页面标题过长。
- 首屏出现大量解释性文字。
- 操作入口分散。
- 用户不知道下一步应该做什么。

## 一阶段基础数据 UI 规划

一阶段基础数据 UI 是本次重构的重点之一。基础数据不是普通表单，而是排产能否运行的前置条件。

建议新增或改造为：

- `基础配置中心`

页面定位：

- 数据准备工作台。
- 告诉用户现在能不能排产。
- 告诉用户缺哪些基础数据。
- 给出处理入口。

### 基础配置中心核心模块

基础配置中心应包含：

- 排产准备度。
- 六步配置进度。
- 阻塞问题。
- 风险提醒。
- 工段配置。
- 设备配置。
- 人员配置。
- 工序映射。
- 导入模板检查。
- 数据健康检查。

### 六步向导

六步向导：

1. 工段配置。
2. 设备配置。
3. 人员配置。
4. 工序映射。
5. 工单导入检查。
6. 运行排产。

每一步显示状态：

- 未开始。
- 进行中。
- 已完成。
- 有风险。
- 阻塞。

每一步显示：

- 当前完成情况。
- 主要问题。
- 处理入口。
- 是否阻塞排产。

### 工段配置 UI

页面目标：

- 建清楚系统有哪些内部工段和外协工段。
- 让 Excel 工序列能稳定映射到系统工段。

字段：

- 工段编码。
- 工段名称。
- 是否外协。
- 默认日产能分钟。
- 默认外协周期小时。
- 状态。

UI 逻辑：

- 工段是第一步。
- 编码必须手填。
- 名称必填。
- 内部工段后续必须有设备。
- 外协工段不要求设备。
- 外协工段显示默认周期。

风险提示：

- 内部工段没有设备：阻塞。
- 工段名称和 Excel 工序列不一致：风险。
- 外协工段没有默认周期：风险或阻塞，取决于是否允许 Excel 工时兜底。

### 设备配置 UI

页面目标：

- 让用户清楚每个内部工段有多少可用设备和日产能。

展示方式：

- 按工段分组。
- 每个工段显示设备数量、启用设备数、总日产能。
- 工段无启用设备时显示阻塞。

字段：

- 设备编码。
- 设备名称。
- 所属工段。
- 日产能分钟。
- 状态：启用、禁用、维修、停机。

UI 逻辑：

- 内部工段必须至少有一台启用设备。
- 停用、维修、停机设备不参与排产。
- 状态变化要影响数据健康检查。

### 人员配置 UI

页面目标：

- 建清楚人员属于哪个工段。
- 为后续执行记录“谁做了这道工序”做准备。

字段：

- 工号。
- 姓名。
- 所属工段。
- 状态。

UI 逻辑：

- 人员不作为当前排产硬约束。
- 工段无人员只提示风险，不阻塞排产。
- 支持页面维护和 Excel 导入。
- Excel 导入兼容 `机台人员` / `机组人员` 横向排班表，第 2 行工段、第 3 行 `工号` / `姓名`；标记为 `外协` 的列不导入人员。
- 停用人员不参与后续派工和人员视图。
- 人员花名册每行都显示该人员的关联工段，避免同组人员只有第一行能看到工段。

页面文案：

- 使用“执行人员”“所属工段”“状态”。
- 避免做成人事系统。

### 工序映射 UI

页面目标：

- 让 Excel 工序列稳定匹配系统工段。

推荐布局：

- 左侧：Excel 工序列。
- 右侧：系统工段。
- 中间或状态列：匹配状态。

状态：

- 已匹配。
- 需确认。
- 外协。
- 未配置。

操作：

- 选择已有工段。
- 创建新工段。
- 标记外协。
- 保存映射规则。

UI 逻辑：

- 未匹配项不能静默导入。
- 用户确认后后续导入复用。
- 工序映射问题应同步出现在导入预览和数据健康检查。

### 工单导入模板检查 UI

页面目标：

- 让用户在入库前知道 Excel 能不能用于排产。

上传前提示：

- 订单号。
- 客户。
- 产品名称。
- 数量。
- 优先级。
- 交期。

上传后显示：

- 新增数量。
- 更新数量。
- 跳过数量。
- 异常数量。
- 零件数量。
- 工序数量。
- 单件工时合计。
- 按零件数量折算后的产能工时。
- 涉及工段。

UI 逻辑：

- 预览阶段不写库。
- Excel 工序数字按单件工时展示，排产驾驶台按产能工时展示。
- 阻塞错误不允许确认入库。
- 提醒问题允许继续，但要明确影响。
- 确认入库后提示下一步进入排产驾驶台。

### 数据健康检查 UI

数据健康检查按三类展示：

- 阻塞。
- 风险。
- 提醒。

每条问题显示：

- 严重程度。
- 问题描述。
- 影响范围。
- 处理入口。

示例：

- 阻塞：工段“焊接”没有启用设备，焊接工序无法排产。
- 风险：工段“气攻”没有人员，排班表人员显示不完整。
- 提醒：设备“龙门-02”已停用，不参与排产。

## 二阶段排产 UI 规划

### 排产驾驶台

`/scheduling` 从“全部队列一键排”升级为“可控排产驾驶台”。

页面结构：

- 本次排产设置。
- 订单选择。
- 执行结果。

“本次排产设置”固定展示：

- 已选订单数。
- 总产能工时。
- 开始日期。
- 是否基于历史方案。
- 是否保留锁定订单。

排产按钮附近必须清楚显示：

- 排哪些订单。
- 从哪天开始排。
- 队列产能工时。
- 排产后会生成新方案，不覆盖旧方案。
- 基于历史方案时，新方案只包含本次勾选订单和历史方案中的已锁订单。

交互逻辑：

- 默认显示全部可排订单（待排/已排）。
- 交期风险订单置顶。
- 已排订单要标识清楚，提示可基于历史方案重排。
- 支持全选、清空和基础筛选。
- 没有选中订单时不能运行排产。
- 存在阻塞问题时提示原因。
- 订单表工时列显示产能工时，避免把单件工时误认为总占用。
- 删除订单入口弱化为“更多”或放到详情页，避免计划员误删。
- 排产成功后不要只显示文字，必须给出“查看订单完工时间”和“进入派工”两个后续动作。

### 订单排产总览

`/schedule-results` 改为“订单完工表”或“订单完工总览”。

页面重点：

- 首屏只保留最关键内容：当前方案、总订单数、延期订单数、最晚完工时间、平均负荷。
- 默认只看订单，老板一眼看“什么时候完工”。
- 每张订单显示预计开始、预计完成、延期天数、瓶颈、锁定状态。
- 订单表字段顺序为：订单号、客户、产品、交期、预计开始、预计完成、延期天数、瓶颈、锁定状态、操作。
- 默认按风险排序：延期订单、临近交期、高优先级、交期早。
- 延期订单红色高亮，临近交期橙色提醒，正常订单用绿色或低调标签。
- 点击订单再看零件和工序。
- 工序明细不要默认压在第一屏。
- 工序级排产明细默认折叠为“查看工序明细”，或只在订单详情页查看。
- 资源负荷概览可以保留，但要放在订单表后面，不抢首屏。
- 锁单操作保留，文案使用“锁定计划 / 取消锁定”，比“锁定 / 解锁”更业务化。

保留入口：

- 甘特图。
- 生产排班表。
- 资源负荷。
- 逾期风险。
- 导出。
- 锁单。

### 订单排产解释

`/scheduling/orders/:workOrderId?schedule_id=...` 从“数据堆叠详情页”升级为“解释为什么这个订单排到这个时间”。

顶部必须先给明确结论：

- 延期时：`预计 05-28 17:00 完工，晚于交期 2 天`。
- 按期时：`预计按期完成`。

第一屏解释内容：

- 最后完成工序：工段、设备、结束时间。
- 影响交付的关键工序：延期时突出最后完成工序；未延期但贴近交期时突出贴近交期的工序。
- 顶部按钮：返回订单完工表、查看排班表定位、查看甘特图。

工序明细表要求：

- 增加序号，体现工序顺序感。
- 增加前置关系，使用当前页面序号解释前置工序。
- 暂不复杂计算等待原因，后续再做关键路径和排队原因。
- 派工状态保留：已派工显示人员、占比和计划工时；未派工显示黄色提醒。

### 生产排班表

`/scheduling/board` 作为侧边栏固定入口，URL 没有方案时默认打开最新排产方案。

`/scheduling/board/:scheduleId` 保留为指定方案的车间排班表。

优化方向：

- 顶部显示当前方案编号、创建时间、订单数、延期数。
- 顶部筛选更紧凑。
- 日期矩阵保持横向滚动。
- 周日和休息日灰底。
- 外协和延期标识明显。
- 任务信息列固定。
- 人员导入不放在排班表内，统一回到 `/personnel` 人员档案页维护。

当前方案互跳要求：

- 从订单完工表进入：`/scheduling/board/:scheduleId`。
- 从派工进入：保留当前方案 ID。
- 从甘特图进入：保留当前方案 ID。
- 从经营问题看板进入：保留问题所属方案 ID。

### 当前方案贯穿

阶段二已落地的统一规则：

- 订单完工表、派工与工时、生产排班表、甘特图、经营问题看板只认一个当前方案。
- URL 有 `schedule_id` 时，用 URL。
- URL 没有时，默认最新方案。
- 页面加载后把实际方案写入当前方案上下文，供侧边导航和页面顶部快捷按钮生成目标地址。
- 页面内切换方案后，后续跳转必须带新方案。

关键跳转：

- 订单完工表 -> `/dispatch?schedule_id=当前方案`
- 订单完工表 -> `/scheduling/board/当前方案`
- 订单完工表 -> `/gantt?schedule_id=当前方案`
- 派工 -> 订单完工表、生产排班表、甘特图时不丢方案。
- 生产排班表 -> 订单完工表、派工、甘特图时不丢方案。
- 甘特图 -> 订单完工表、派工、生产排班表时不丢方案。
- 经营问题看板 -> 订单完工表、派工、生产排班表、甘特图时不丢方案。

## 三阶段经营问题看板 UI 规划

新增：

- `/management-dashboard`

页面定位：

- 老板看交付风险。
- 调度员看问题处理状态。
- 默认不是图表，而是问题清单。

第一屏显示：

- 高风险订单数。
- 已延期订单数。
- 预计延期订单数。
- 瓶颈资源数。
- 外协风险数。
- 未处理问题数。
- 最紧急问题清单。

问题列表字段：

- 风险等级。
- 问题类型。
- 订单号。
- 客户。
- 交期。
- 预计完成。
- 原因。
- 建议动作。
- 处理状态。
- 备注。

交互：

- 修改处理状态。
- 编辑备注。
- 跳转订单详情。
- 跳转排班表。
- 导出 Excel。

页面要求：

- 第一屏直接显示问题。
- 不做全屏大屏。
- 不用复杂图表替代问题清单。
- 高风险和未处理问题必须醒目。

## 组件拆分规划

建议新增组件目录。

### components/layout

- `MainLayout`
- `SidebarNav`
- `PageHeader`
- `StepIndicator`

职责：

- 页面框架。
- 导航。
- 页面标题。
- 当前流程步骤。

### components/common

- `StatusBadge`
- `SummaryCards`
- `CurrentScheduleBanner`
- `FilterBar`
- `DataState`
- `SectionPanel`

职责：

- 通用状态。
- 摘要卡。
- 当前方案摘要。
- 筛选条。
- 加载、空、失败状态。
- 通用分区容器。

### components/master-data

- `SetupProgress`
- `DataHealthPanel`
- `WorkCenterSetupTable`
- `MachineSetupTable`
- `PersonnelSetupTable`
- `OperationMappingTable`

职责：

- 一阶段基础数据准备。
- 数据健康检查。
- 工段、设备、人员、映射维护展示。

### components/scheduling

- `ScheduleRunForm`
- `OrderGanttChart`
- `RiskOrderTable`
- `ResourceLoadPanel`
- `ScheduleResultLinks`

职责：

- 二阶段可控排产。
- 订单级结果。
- 资源负荷。
- 结果入口。

### components/management / delivery-risk

- `IssueSummaryCards`
- `IssueList`
- `IssueStatusEditor`
- `ManagementRiskTable`

职责：

- 三阶段交付风险看板（原经营问题看板）。
- 风险问题列表。
- 状态和备注维护。

组件拆分目标：

- 页面负责组织业务流程。
- 组件负责展示。
- API 聚合计算仍由后端完成。
- 不让前端重新计算延期、负荷、瓶颈。

## 样式体系规划

继续使用纯 CSS，不引入重 UI 库。

建议整理为：

- `layout.css`
- `forms.css`
- `tables.css`
- `scheduling.css`
- `dispatch.css`
- `board.css`
- `management.css`

如果暂时不拆文件，也要在 `styles.css` 内按模块分区。

统一风格规则：

- 页面背景：浅灰。
- 主内容：白底或极浅灰。
- 边框：浅灰细线。
- 圆角：控制在 6-8px。
- 阴影：少用，弱化。
- 表格优先，卡片适量。
- 字号紧凑但可读。
- 状态色统一。
- 操作按钮不使用大面积鲜艳色。
- 危险动作和风险提示必须醒目。

### 状态样式

统一状态：

- 阻塞：红色。
- 高风险：红色。
- 预警：橙色。
- 正常：绿色。
- 信息：蓝灰。
- 禁用：灰色。

### 表格样式

表格要求：

- 行高稳定。
- 表头清楚。
- 数字右对齐或统一格式。
- 关键字段加粗。
- 状态列使用标签。
- 操作列位置固定。

### 表单样式

表单要求：

- 标签明确。
- 必填项明显。
- 错误提示靠近字段。
- 主要按钮固定在表单末尾。
- 不把所有字段塞进一行。

## 路由与菜单规划

菜单按业务流程重排。

### 数据准备

- 基础配置中心。
- 工单导入。
- 工序映射。

### 排产执行

- 排产驾驶台。
- 生产排班表。

### 结果分析

- 订单完工表。
- 甘特图。
- 资源负荷。
- 逾期风险。

### 经营问题

- 经营问题看板。
- 生产排班表。

### 系统基础

- 设备管理。
- 人员管理。
- 旧 Demo 入口折叠或弱化。

旧 Demo 页面不立即删除，但不作为主导航重点。

## 分阶段实施顺序

### 已完成：阶段一，统一主流程

目标：让用户一进系统就知道下一步做什么。

落地内容：

- `/` 改为“计划员工作台”。
- 首页不再做综合大屏，只展示是否可以排产、阻塞项数量、可排订单数、最新排产方案、延期订单数、未派工任务数。
- 首页根据数据状态给出唯一主按钮：
  - 没基础数据 -> 去基础配置。
  - 没工单 -> 去工单导入。
  - 有工单没方案 -> 去排产驾驶台。
  - 有方案 -> 查看订单完工表。
- 主导航改为计划员主线：
  - 运行总览
  - 工单导入
  - 排产驾驶台
  - 订单完工表
  - 派工与工时
  - 生产排班表
  - 甘特图
  - 基础配置

### 已完成：阶段二，当前方案贯穿全局

目标：用户只认一个“当前方案”，不同页面不要各选各的。

落地内容：

- 增加当前方案路径工具，统一生成带 `schedule_id` 的页面跳转地址。
- 增加 `CurrentScheduleBanner`，展示当前方案编号、创建时间、订单数、延期数。
- `ScheduleResults.jsx`、`Dispatch.jsx`、`ScheduleBoard.jsx`、`Gantt.jsx`、`ManagementDashboard.jsx` 均接入当前方案摘要。
- 侧边导航和页面顶部快捷按钮接入当前方案跳转逻辑。
- URL 优先，URL 没有时默认最新方案。

### 已完成：阶段三，排产驾驶台流程优化

目标：排产动作更像一个清晰命令，而不是一堆表格按钮。

落地内容：

- `Scheduling.jsx` 页面结构改为“本次排产设置 / 订单选择 / 执行结果”三块。
- 本次排产设置固定展示已选订单数、总产能工时、开始日期、是否基于历史方案、是否保留锁定订单。
- 运行排产按钮旁明确说明：会生成新方案、不覆盖历史方案；基于历史方案时只包含勾选订单和已锁订单。
- 排产成功后给出“查看订单完工时间”和“进入派工”。
- 默认显示全部可排订单，交期风险订单置顶，已排订单标识清楚。
- 删除订单按钮从主表弱化到“更多”，主操作改为详情。

### 已完成：阶段四，订单完工表重构

目标：老板和计划员第一眼看清“什么时候完工”。

落地内容：

- `ScheduleResults.jsx` 命名和页面重点改为“订单完工表”。
- 首屏只保留当前方案、总订单数、延期订单数、最晚完工时间、平均负荷。
- `OrderTimeline.jsx` 从订单甘特改为订单完工主表。
- 主表字段顺序调整为订单号、客户、产品、交期、预计开始、预计完成、延期天数、瓶颈、锁定状态、操作。
- 默认按延期、临近交期、高优先级、交期早排序。
- 延期红色、临近交期橙色、正常低调绿色。
- 工序明细默认收起，资源负荷概览下移到订单表之后。
- 锁单文案改为“锁定计划 / 取消锁定”。

### 已完成：阶段五，订单详情优化

目标：计划员点进详情不是看数据堆叠，而是看解释。

落地内容：

- `OrderScheduleDetail.jsx` 顶部增加明确完工结论。
- 增加最后完成工序解释：工段、设备、结束时间。
- 增加影响交付的关键工序。
- 工序明细表增加序号和前置关系。
- 派工状态保留：已派工显示人员、占比和计划工时；未派工显示黄色提醒。
- 顶部按钮统一为返回订单完工表、查看排班表定位、查看甘特图。

### 已完成：阶段六，派工流程优化

目标：排产后更快处理未派工任务。

落地内容：

- `Dispatch.jsx` 默认按未派工优先组织任务，排序口径为未派工、交期早、开始时间早。
- 顶部摘要增加当前方案、任务总数、未派工数、已派工数、已分摊工时。
- 左侧任务列表继续点击选择任务，右侧人员分摊保持原有编辑结构。
- 保存成功后自动跳到下一条未派工任务，让派工从“编辑某条任务”变成“连续处理未派工任务”。
- 增加快捷操作：单人 100%、多人均分。
- 人员下拉显示姓名、工号、所属工段，并优先推荐当前工段人员。
- 如果人员不属于当前工段，只给提醒，不阻止保存。
- 该阶段不改变后端保存校验：人员仍需在职、不能重复、占比合计必须为 100%。

### 已完成：阶段七，生产排班表优化

目标：排班表成为现场复核工具，不承担配置功能。

落地内容：

- `ScheduleBoard.jsx` 默认打开当前方案；没有当前方案上下文时才回退到最新方案。
- 默认日期范围从方案开始日期起，默认 14 天。
- 筛选栏压缩为：视图、工段、工单、日期跨度、搜索。
- 视图保留工段、设备、人员三种模式。
- 人员视图中未派工分组置顶，已派工人员按姓名排序。
- 日期矩阵继续保留横向滚动，任务信息列继续固定。
- 单元格显示工时；鼠标悬停显示订单、工序、时间段。
- 外协、延期、休息日颜色继续保留，但降低视觉重量，避免干扰现场复核。
- 移除排班表中的人员配置导向，人员维护仍回到人员档案页。

### 已完成：阶段八，经营问题看板弱化但保留

目标：满足老板看风险，但不抢计划员主流程。

落地内容：

- `/management-dashboard` 页面显示名改为“交付风险看板”，接口路径仍沿用 `management-dashboard`。
- 导航位置移到靠后的“管理查看”分组，不放在计划员主线前半段。
- 页面默认仍显示问题清单，不改成大屏。
- 问题清单字段顺序调整为：订单号、客户、交期、预计完成、延期天数、原因、当前处理状态、风险、定位。
- 摘要卡、最紧急问题、风险分布下移，作为辅助信息。
- 保留按方案、未来天数、风险等级、问题类型、客户、处理状态筛选。
- 保留状态备注、下钻订单/生产排班表/甘特图、导出 Excel。
- 不新增复杂负责人、日志、审批功能。

### 已完成：阶段九，文案统一

目标：减少用户理解成本。

落地内容：

- “订单排产总览”统一为“订单完工表”。
- “排产驾驶台”“生产排班表”“派工与工时”继续保留。
- “经营问题看板”在前端显示为“交付风险看板”。
- “锁定 / 解锁”统一为“锁定计划 / 取消锁定”。
- “产能工时”统一补充“按零件数量折算”说明。
- “单件工时”只在导入预览相关区域出现，避免计划员误解为总占用。

### 已完成：阶段十，代码整理（前端优先）

目标：为后续维护降复杂度，不改变功能。

落地内容：

- `ScheduleResults.jsx` 拆出：
  - `ScheduleSelector.jsx`
  - `OrderCompletionTable.jsx`
  - `OrderLockTable.jsx`
  - `ResourceLoadPanel.jsx`
  - `ScheduleOperationDetail.jsx`
- `Dispatch.jsx` 拆出：
  - `DispatchFilterBar.jsx`
  - `DispatchTaskTable.jsx`
  - `PersonnelAllocationEditor.jsx`
  - `PersonnelWorkloadTable.jsx`
- `WorkCenters.jsx` 拆出：
  - `WorkCenterForm.jsx`
  - `WorkCenterTable.jsx`
  - `MachineTable.jsx`
  - `EditWorkCenterModal.jsx`
- 页面继续负责取数、状态和事件编排；组件负责展示和局部交互。
- 后端服务拆分、样式文件拆分暂不进入本次改动。

已验证：

- `npm run build` 通过。

### 后续：前端骨架继续收敛

- 拆 `App.jsx`。
- 抽离导航配置。
- 抽离页面 meta。
- 建立统一 `MainLayout`。
- 保持现有路由可用。

### 后续：UI 风格继续统一

- 收敛颜色、间距、按钮、表格、状态标签。
- 去掉过重装饰。
- 保持简约清晰。
- 优化页面首屏信息密度。

### 后续：基础数据 UI 继续重构

- 新增基础配置中心。
- 增加六步向导。
- 增加数据准备度。
- 增加健康检查。
- 优化工段、设备、人员、工序映射入口。

### 后续：交付风险看板继续打磨

- 继续沿用 `/management-dashboard`。
- 保持默认问题清单，不改成大屏。
- 支持状态、备注、导出。
- 保持老板视角清晰，但不新增复杂负责人、日志、审批。

## Test Plan

文档完成后检查：

- 是否明确 UI 风格为简约、清晰、步骤明确。
- 是否覆盖一阶段基础数据填写逻辑。
- 是否包含基础配置中心和六步向导。
- 是否明确阻塞、风险、提醒的区别。
- 是否没有把人员当作当前排产硬约束。
- 是否明确二阶段排产页面逻辑。
- 是否明确三阶段交付风险看板页面逻辑。
- 是否明确组件拆分和样式整理方向。
- 是否保持 React + Vite + Axios + 纯 CSS。
- 是否不要求一次性推翻现有页面。

后续实现验收：

- 原有页面仍可访问。
- 新菜单按业务流程组织。
- 用户能从基础数据一路走到排产结果。
- 基础配置中心能显示准备度和问题。
- 排产驾驶台能说明当前排哪些订单、从哪天开始。
- 排产驾驶台能说明会生成新方案、不覆盖历史方案。
- 订单完工表默认只展示订单级结果，能一眼看到预计完成时间。
- 订单详情页顶部能解释预计完工结论、最后完成工序和关键工序。
- 派工页默认未派工优先，保存后能连续定位下一条未派工任务。
- 派工页人员下拉能显示姓名、工号、所属工段，跨工段人员只提醒不阻止。
- 生产排班表默认打开当前方案，并从方案开始日期展示 14 天。
- 生产排班表人员视图中未派工置顶，已派工人员按姓名排序。
- 交付风险看板默认展示问题清单，且导航位置靠后。
- 页面中文文案简洁专业。
- `npm run build` 通过。

## Assumptions

- 当前文档既记录规划，也记录 2026-05-21 已落地的前端阶段一至阶段十。
- 前端重构仍分阶段进行。
- 旧 Demo 页面可以弱化，但不立即删除。
- 新 UI 以 APS v2 `production_*` 主线为准。
- 前端不承担复杂排产聚合计算，订单完工、延期、负荷、瓶颈仍由后端返回。
